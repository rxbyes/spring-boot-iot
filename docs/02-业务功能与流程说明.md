# 02 业务功能与流程说明

> 文档定位：业务语义、角色流程和工作台组织说明。
> 适用角色：产品、研发、测试、交付、帮助中心维护者。
> 权威级别：一级权威。
> 上游来源：当前代码、菜单结构、验收基线、业务页面与接口实现。
> 下游消费：帮助中心业务稿、需求拆解、交付矩阵、阶段规划。
> 变更触发条件：业务语义、角色流程、工作台结构、系统内容消费口径变化。
> 更新时间：2026-04-04

职责边界：
- 本文件只负责业务语义、角色流程和工作台口径。
- 当前纳入交付的能力矩阵与验收判定，以 [21-业务功能清单与验收标准.md](./21-业务功能清单与验收标准.md) 为准。
- Phase 4 是否计入正式交付、当前阻塞和复验状态，以 [19-第四阶段交付边界与复验进展.md](./19-第四阶段交付边界与复验进展.md) 为准。
- 多租户、组织范围与数据权限，以 [13-数据权限与多租户模型.md](./13-数据权限与多租户模型.md) 为准。

## 1. 业务域划分

后端业务仍保持“三大业务域”，前端导航则按角色工作场景重组为“五大工作台”：

1. 后端设备接入域：产品、设备、协议、消息、属性、下行指令。
2. 后端风险处置域：告警、事件、工单、风险点、阈值规则、联动规则、应急预案、分析报表。
3. 后端系统治理域：组织、用户、角色、菜单、区域、字典、通知渠道、审计日志。
4. 前端五大工作台：`接入智维`、`风险运营`、`风险策略`、`平台治理`、`质量工场`。

### 1.1 三域关系总览

三大后端业务域的关系不是并列平铺，而是稳定的“数据源 -> 业务闭环 -> 治理支撑”关系：

1. `IoT 主链路`
   - 负责产品、设备、物模型、属性/遥测、消息日志，是平台上游数据源。
2. `风险平台`
   - 消费 IoT 主链路结果，完成风险点、规则、告警、事件、工单闭环。
3. `系统管理`
   - 提供组织、区域、字典、通知渠道、审计日志等治理底座。

主关系固定为：

1. `IoT 主链路 -> 风险平台`
2. `系统管理 -> IoT 主链路`
3. `系统管理 -> 风险平台`
4. `风险平台 -> IoT 主链路`

## 2. 当前可用能力清单

### 2.1 设备接入域

- 产品定义中心当前闭环：分页筛选、详情查看、新增、编辑、单删、CSV 导出、按产品查看关联设备库存。
- 产品物模型设计器当前继续挂在 `/products` 产品定义中心内，不新增一级路由；同一抽屉当前固定为“规范证据优先治理”和“正式模型”两类工作视图。
- 设计器抽屉当前已升级为大工作台结构：统一采用“舞台头部层 + 总览指标层 + 主体工作区层”，默认流程固定为“规范证据预设 / 报文证据 compare / 显式 apply / 正式模型回刷”；治理视图固定为“证据入口 + 治理摘要 + 对比工作区 + 正式模型回刷”，正式模型视图固定为“正式契约总览 + 类型概览条 + 统一资产卡列表”。
- 规范证据当前首批只服务于当前选中的产品，默认预设编码为 `landslide-integrated-tilt-accel-crack-v1`；工作台与抽屉默认按“倾角 / 加速度 / 裂缝一体机”预设进入治理，并默认勾选倾角核心字段 `L1_QJ_1.X / Y / Z / angle / AZI`。正式 compare 行围绕规范化 `identifier` 建行，不再围绕厂家原始字段名直接建正式契约。
- 单设备样本 JSON 与人工补录当前继续保留，但语义已降级为辅助证据入口：样本 JSON 默认只用于人工核对，不再作为规范模式下的 compare 主入口；业务数据、状态数据和其他数据仍可手工粘贴提炼，数组与空对象会按结构噪音忽略。同时允许通过人工补录把 `property / event / service` 三类候选纳入同一治理会话。
- 报文证据当前通过治理 compare 入口并列展示：属性候选优先来自 `iot_device_property` 并辅以 `iot_device_message_log` 最近真实上报，并按规范预设做字段别名归一；事件 / 服务只有存在真实证据时才会生成候选；旧 `GET /model-candidates` 仍保留为底层兼容能力，但不再是唯一 UI 入口。
- 正式模型维护当前继续提供 `property / event / service` 三类物模型的列表、新增、编辑、删除、类型切换和 JSON 合法性提示；只有人工确认过的治理决策才会写入 `iot_product_model`，其中“其他数据”样本提炼出的字段默认标记 `needsReview`，`continue / ignore` 决策不会落库。
- 产品详情当前额外提供设备活跃度统计：今日活跃、7 日活跃、30 日活跃，以及平均在线时长、最长在线时长。
- 设备新增/查询（按 `id`、按 `deviceCode`）。
- 设备资产中心第三轮闭环：分页筛选、详情查看、新增、编辑、批量导入、设备更换、单删、批量删除、CSV 导出、父设备/子设备拓扑维护，以及“已登记设备 + 未登记上报名单”同页联查。
- `2026-04-03` 起，设备资产中心已从“仅租户侧治理”升级为“租户 + 主机构/机构子树治理”：`iot_device` 当前正式落库 `org_id / org_name`，新增、编辑、批量导入与设备更换生成新设备时，后端统一按当前登录人的主机构回填归属；详情、分页、设备选项、测点选项、属性读取、消息日志、链路追踪分页/统计、失败归档分页/统计/详情，以及增改删/更换/批量导入/批量删除当前都统一透传当前登录用户主键，并按 `tenantId + dataScopeType + orgId` 收口。设备域当前语义固定为：`ALL / TENANT = 当前租户全部设备`、`ORG_AND_CHILDREN = 当前主机构及子树设备`、`ORG = 当前主机构设备`、`SELF = 当前等价收敛为 ORG`；未登记上报名单在组织受限场景下不再跨机构回流。
- 链路验证中心模拟上报（HTTP / MQTT 双通道，设备编码反查接入契约）。
- MQTT 上行接入（标准 Topic + `$dp` 兼容）。
- `mqtt-json` 协议解码。
- 消息日志持久化与查询。
- 最新属性更新。
- 设备在线状态更新。
- 下行发布最小能力（MQTT publish）。
- 消息追踪（按 TraceId/设备/Topic 检索）。
- 接入处理时间线复盘（按 `sessionId / traceId` 查看固定 Pipeline 阶段、耗时、处理类/方法与摘要）。

#### 2.1.0 IoT 五层治理模型

`产品 -> 设备 -> 物模型 -> 属性/遥测 -> 消息日志` 是当前设备接入域的固定治理顺序，不应被理解为彼此独立的并列对象：

1. `产品`
   - 定义一类设备的接入模板和业务身份。
2. `设备`
   - 定义某台具体资产实例。
3. `物模型`
   - 定义平台认可的正式字段标准。
4. `属性/遥测`
   - 定义运行期最新值和历史值。
5. `消息日志`
   - 定义原始报文证据和处理留痕。

最小治理规则：

1. 先建产品，再建设备。
2. 同一产品下正式字段 `identifier` 必须唯一。
3. 核心判定字段必须同时明确 latest 与 telemetry 口径。
4. 成功与失败上报都必须保留消息日志。
5. 父设备 / 子设备场景必须明确风险责任归属。

详细字段治理表与业务建档联调步骤，参考 [appendix/iot-field-governance-and-sop.md](./appendix/iot-field-governance-and-sop.md)。

补充口径：
- `/reporting` 当前已升级为“结果复盘优先”的链路验证中心：默认先进入 `结果复盘`，通过共享诊断状态头统一表达 `尚未验证 / 验证成功 / 等待回流 / 验证失败 / 复盘受限`，并继续保留 `模拟验证 / 结果复盘 / 最近记录` 三个真实业务页签。
- `deviceCode` 仍是链路验证中心唯一主查询入口；查询成功后前端只读反显 `productKey`、`protocolCode`、`clientId=deviceCode`。
- 页面保留 `tenantId` 与 `topic` 可编辑，其中 `tenantId` 默认 `1` 且仅 HTTP 提交生效；`topic` 默认 `$dp`，HTTP / MQTT 均允许手工覆盖。
- 明文模式下，只有可识别 JSON 才参与 C.1 / C.2 / C.3 判定与帧构造；XML 与普通文本仅提供格式化和原样发送，不新增 XML 协议解析。
- 当传输方式为 MQTT、`topic=$dp` 且明文内容为普通属性 JSON 且缺少设备身份字段时，前端会在实际发送 payload 中自动补入 `deviceCode`，保证默认兼容链路可用。
- 发送成功后，页面会自动切回 `结果复盘` 页签，并在共享诊断状态头和“处理时间线”区域同时表达同一次提交的固定 Pipeline 复盘：HTTP 直接展示 timeline，MQTT 先显示“等待消费回流”，绑定 `traceId` 后再展示完整时间线。
- `最近记录` 当前已从“恢复入口”升级为诊断清单：每条记录优先展示 verdict、当前卡点和建议动作，再提供恢复当前复盘上下文的入口。
- `/message-trace` 当前详情抽屉会按 `traceId` 异步拉取同一条 Redis 时间线；若时间线已过期，页面明确提示“时间线已过期，仅保留消息日志”。
- `接入智维` 当前已通过共享诊断上下文把 `/reporting`、`/message-trace`、`/system-log`、`/file-debug` 与失败归档模式串成首批排查流；跨页只透传高价值定位字段与来源页，不新增后端聚合接口或第二套路由骨架。
- 当诊断结果指向契约治理或资产治理时，页面可直接跳入 `产品定义中心` 或 `设备资产中心`；治理页当前只在工作台内联提示“来自哪个诊断页 + 优先核对什么”，不再额外堆介绍墙、概况卡或重复导流文案。
- 2026-03-28 起，`接入智维` 7 个入口当前统一收口为“轻量页面壳 + 真业务页签/单主内容区”结构：总览=`资产底座 / 诊断排障`，产品=`单主列表页（物模型设计器与关联设备回收到行操作和抽屉）`，设备=`单主列表页（未登记资产与拓扑治理回收到同一主台账）`，链路验证=`模拟验证 / 结果复盘 / 最近记录`，异常观测=`单主列表页（通过顶部动作回跳链路追踪和失败归档）`，链路追踪=`链路追踪 / 失败归档`，数据校验=`设备校验 / 原始响应`。

#### 2.1.1 产品与设备的逻辑关系

- 产品是平台里的“接入模板”，不是某一台具体硬件。一个产品定义了一组设备共同遵循的 `productKey`、`protocolCode`、`nodeType`、`dataFormat`、`manufacturer` 和物模型边界。
- 设备是某个产品下的具体资产实例。设备补充的是 `deviceCode`、鉴权凭据、固件版本、IP、安装位置、在线状态、最近上报时间等实例信息。
- 平台关系可以简化为：`1 个产品 -> N 台设备 -> N 条消息 / 属性 / 命令记录`。同一产品下可以有很多设备，但设备不能脱离产品单独定义接入协议。
- 是否应该拆成新产品，不看“是不是同一个项目”，而看“接入契约是否一致”。协议编码、Topic/报文结构、物模型、认证方式、下行指令、节点类型或生命周期策略任一项发生变化，都应评估拆成不同产品。

#### 2.1.2 建档顺序与治理口径

| 规则 | 说明 | 当前状态 |
|---|---|---|
| 先定义产品身份与协议，再允许设备建档 | 设备建档时必须先通过 `productKey` 找到产品，再继承产品的协议编码与节点类型 | 已落地 |
| `productKey` 创建后不允许修改 | `productKey` 是接入身份，变更后会影响建档、Topic 和日志检索 | 已落地 |
| 删除产品前先清理库存设备 | 产品删除前会校验是否还有关联设备库存 | 已落地 |
| 停用产品前先核查库存设备是否仍在使用 | 停用前会校验该产品下是否仍存在启用中的关联设备，避免存量在用设备继续挂在停用产品下 | 已落地 |

补充说明：
- 当前“删除产品”会校验全部未删除库存设备；“停用产品”会校验仍处于启用状态的关联设备。
- 停用产品后，系统会拒绝新增设备、批量导入、设备更换目标建档、设备上报接入以及 MQTT 下行指令下发；`GET /api/device/product/list` 也只返回启用产品。
- 若当前设备需要纳入父子拓扑，新增/编辑/设备更换场景可直接维护 `parentDeviceId`；批量导入场景可通过 `parentDeviceCode` 建立父子关系。设备详情、列表和导出会同步展示父设备与关联网关信息，便于资产中心直接核对拓扑归属。
- 设备资产中心当前新增 `登记状态` 口径：`已登记` 行来自 `iot_device` 主档；`未登记` 行来自最近一次未建档上报失败归档，默认优先取 `iot_device_access_error_log` 最近一条失败记录，并按设备补齐仅存在 `iot_device_message_log(messageType=dispatch_failed)` 的最近上报；若共享环境尚未补齐 access-error 表或列结构，则整体回退到 `dispatch_failed` 失败轨迹。
- 未登记上报名单当前只用于“发现设备、补档建账、定位失败阶段”，因此在 `/devices` 中保持只读：可查看详情、失败阶段、Topic、TraceId 与原始载荷，但不支持编辑、设备更换、删除或跳转对象洞察。
- `/devices` 详情抽屉当前固定采用独立 `DeviceDetailWorkbench` 扁平工作台：已登记设备按“顶部轻摘要带 + 中段双轴台账 + 底部补充区”展开，未登记设备按同一节奏展示“摘要带 + 来源档案与失败摘要 + 最近载荷”，不再把身份、认证、关系和元数据拆成多张同权重卡片。

#### 2.1.3 产品字段定义口径

| 字段 | 本质定义 | 建议写法 |
|---|---|---|
| `productKey` | 机器侧接入身份标识，用于产品唯一识别、设备建档、MQTT Topic、消息日志和审计追踪 | 使用固定英文小写编码，推荐模式：`{vendorCode}-{scene}-{category}[-{protocolOrVersion}]`；不要包含中文、空格、项目名、站点名、客户名等易变信息 |
| `productName` | 面向人读的人类可理解名称，用于列表、详情、运营协作与报表识别 | 推荐模式：`厂商 + 场景/用途 + 品类 [+ 协议/系列/版本]`，例如 `南方测试 监测型 GNSS`、`中海达 预警型 爆闪灯终端` |
| `manufacturer` | 厂商品牌/制造商的标准名称，用于主数据归类 | 只填厂商，如 `南方测试`、`中海达`；不要写成 `中海达GNSS`、`南方测试雨量计项目` 这类混合值 |

补充规则：
- `vendorCode` 需要团队先约定并保持稳定，例如统一使用 `nf` 表示南方测试、`zhd` 表示中海达；不要今天用 `south`，明天改成 `nf`。
- 如果同一厂商、同一品类下存在多个协议版本或 payload 版本，建议在 `productKey` 末尾加版本后缀，如 `-v1`、`-v2` 或 `-mqttjson-v1`。
- 同一项目、同一站点里的多台同类设备，应共用一个产品，把序列号、安装点位、区域、设备编号放到设备维度维护。
- 如果只是换了项目名称、客户名称、安装位置，不应新建产品；如果换了协议、物模型或下行指令，则应评估新建产品。

#### 2.1.4 结合你当前场景的建议拆分方式

| 厂商 | 场景 | 品类 | 建议 `productKey` | 建议 `productName` | 建议 `manufacturer` | 拆分理由 |
|---|---|---|---|---|---|---|
| 南方测试 | 监测 | GNSS | `nf-monitor-gnss-v1` | `南方测试 监测型 GNSS` | `南方测试` | GNSS 监测上报字段、告警阈值和物模型通常独立 |
| 南方测试 | 监测 | 深部位移 | `nf-monitor-deep-displacement-v1` | `南方测试 监测型 深部位移仪` | `南方测试` | 深部位移与 GNSS 的测点结构、属性模型不同 |
| 南方测试 | 监测 | 雨量计 | `nf-monitor-rain-gauge-v1` | `南方测试 监测型 雨量计` | `南方测试` | 雨量采集周期、指标含义和告警口径不同 |
| 中海达 | 监测 | GNSS | `zhd-monitor-gnss-v1` | `中海达 监测型 GNSS` | `中海达` | 与其他品类相比，GNSS 物模型和接入语义独立 |
| 中海达 | 监测 | 多维 | `zhd-monitor-multidim-v1` | `中海达 监测型 多维位移仪` | `中海达` | 多维位移设备通常拥有独立测轴和属性集合 |
| 中海达 | 监测 | 雨量计 | `zhd-monitor-rain-gauge-v1` | `中海达 监测型 雨量计` | `中海达` | 与监测 GNSS、多维设备的物模型和规则不同 |
| 中海达 | 预警 | 广播 | `zhd-warning-broadcast-v1` | `中海达 预警型 广播终端` | `中海达` | 广播设备更偏控制/播报指令，不应与监测类产品混在一起 |
| 中海达 | 预警 | 爆闪灯 | `zhd-warning-strobe-v1` | `中海达 预警型 爆闪灯终端` | `中海达` | 爆闪灯的下行控制能力和状态字段独立 |
| 中海达 | 预警 | 情报板 | `zhd-warning-info-board-v1` | `中海达 预警型 情报板` | `中海达` | 情报板通常包含文本/版式下发，与广播、爆闪灯不同 |

进一步建议：
- 如果“中海达监测型 GNSS”有多种硬件型号，但协议、Topic、字段和命令完全一致，可以继续共用一个产品，在 `description` 或设备备注里区分型号。
- 如果同一品类后来切换了新的协议版本、密钥体系或物模型，请新建 `v2` 产品，不要在已有设备存量上直接改 `productKey`。
- 项目名称、库区名称、边坡名称、监测点名称都不建议进入 `productKey` 和 `manufacturer`；这些信息应维护在设备、组织、区域或风险点维度。

### 2.2 风险预警处置域

- 告警中心：列表、详情、确认、抑制、关闭。
- 事件处置：列表、详情、派发、接收、开始、完成、反馈、关闭。
- 风险点管理：CRUD、设备绑定/解绑；新增时风险点编号按“所属组织 + 风险点名称 + 风险点档案等级”自动生成，并避开同租户内已软删除历史记录占用的编号；编辑时保留原编号。风险点档案等级统一使用平台治理 `risk_point_level` 字典返回的 `level_1 / level_2 / level_3`，页面展示为“一级风险点 / 二级风险点 / 三级风险点”；运行态风险态势单独使用 `currentRiskLevel` 的红/橙/黄/蓝四色口径，历史 `riskLevel` 仅作为兼容镜像保留。表单同时维护所属组织、所属区域和负责人，创建人/更新人编号仅在编辑态只读展示。2026-04-02 起，风险点列表、分页、详情、删除、绑定/解绑和绑定设备查询已按当前登录人的 `tenant + dataScopeType + 主机构` 收口第一批查询侧权限；2026-04-03 起，绑定设备弹窗只返回“当前可见范围内、与风险点同机构、且未被其他风险点占用”的设备，当前风险点已绑定设备允许继续回显；同一台设备不能同时绑定多个风险点，但同一风险点下仍允许绑定多个测点。
- 风险点管理：CRUD、设备绑定/解绑；新增时风险点编号按“所属组织 + 风险点名称 + 风险点档案等级”自动生成，并避开同租户内已软删除历史记录占用的编号；编辑时保留原编号。风险点档案等级统一使用平台治理 `risk_point_level` 字典返回的 `level_1 / level_2 / level_3`，页面展示为“一级风险点 / 二级风险点 / 三级风险点”；运行态风险态势单独使用 `currentRiskLevel` 的红/橙/黄/蓝四色口径，历史 `riskLevel` 仅作为兼容镜像保留。表单同时维护所属组织、所属区域和负责人，创建人/更新人编号仅在编辑态只读展示。2026-04-02 起，风险点列表、分页、详情、删除、绑定/解绑和绑定设备查询已按当前登录人的 `tenant + dataScopeType + 主机构` 收口第一批查询侧权限；当只拿到“风险点名称 + 设备编号”、尚未确认 `metricIdentifier / metricName` 时，关系必须先落 `risk_point_device_pending_binding` 做待治理留痕，不能直接写正式 `risk_point_device`。
- 阈值规则：CRUD + 运行期判级策略来源；启用中的规则必须提供可执行表达式。
- 联动规则：CRUD + 自动闭环命中留痕来源。
- 应急预案：CRUD + 自动闭环场景化匹配来源。
- 报表分析：风险趋势、告警统计、事件闭环、设备健康。

### 2.3 系统治理域

- 组织管理（树结构 + CRUD；列表、分页、树和详情已按当前登录人的租户与数据范围收口第一批查询侧权限）。
- 用户管理（CRUD + 密码重置 + 主机构绑定 + 角色绑定；列表、分页、详情以及新增/编辑/删除/重置密码已按当前登录人的租户与数据范围收口第一批治理权限。2026-04-02 起，新增/编辑抽屉的“主机构”固定复用 `GET /api/organization/writable-tree`，`ORG` 范围账号可继续选择当前主机构下的二级、三级公司；前端不再把 `orgId / roleIds` 强转为 `Number`，避免 19 位 Long ID 在多角色编辑时失真。）
- 角色管理（CRUD + 用户角色关系查询 + 数据范围维护；2026-04-03 起，列表、分页、详情、按用户查角色以及新增/编辑/删除已统一按当前登录人的租户上下文收口，不再允许跨租户读取或改写角色档案）。
- 账号中心（当前登录用户基础资料维护 + 机构角色摘要 + 安全信息 + 实名资料）。
- 菜单管理（树 + 列表 + CRUD）。
- 区域管理（树 + 列表 + CRUD；2026-04-03 起，列表、分页、树、详情以及新增/编辑/删除已按当前登录人的租户上下文收口。）
- 字典管理（字典 + 字典项；2026-04-03 起，字典分类与字典项的查询、详情、新增、编辑、删除已按当前登录人的租户上下文收口。）
- 通知渠道管理（CRUD + 渠道测试；2026-04-03 起，列表、分页、按编码查询、新增/编辑/删除与测试通知已按当前登录人的租户上下文收口。）
- 站内消息管理（消息编排、已读状态、当前用户消息分页与未读统计）。
- 帮助文档管理（业务类、技术类、FAQ 三类资料编排与权限过滤访问；2026-04-03 起，管理端分页、详情、新增/编辑/删除与消费端 `access/list`、`access/page`、`access/{id}` 已统一先按当前登录人的租户边界收口。）
- 审计日志管理（业务审计 + system_error 统计；2026-04-03 起，列表、分页、统计、详情、删除已按当前登录人的租户上下文收口。）
- 高优未读桥接（可选开启）：当 `high / critical` 站内消息超过阈值仍未读时，复用既有通知渠道向外桥接提醒。

## 3. 核心业务流程

### 3.1 设备接入主流程（HTTP）

1. 先创建产品与物模型，明确 `productKey`、协议、节点类型、数据格式和厂商。
2. 创建设备并绑定产品。
3. 设备经 `/api/message/http/report` 上报。
4. 协议层标准化上报消息。
5. 写入消息日志与最新属性。
6. 更新设备在线/最近上报时间。

### 3.2 设备接入主流程（MQTT）

1. 设备按标准 Topic 或 `$dp` 上报。
2. 解析 Topic 中的 `productKey` 与 `deviceCode`，鉴别直连/子设备/兼容模式。
3. 协议适配器完成解码、安全校验、必要解密。
4. 写入日志与属性，刷新在线状态。
5. 异常链路记录为 `system_error` 审计，并可触发现有 webhook/企业消息通知。

### 3.3 告警与事件闭环流程

1. 规则触发告警（`iot_alarm_record`）。
2. 告警确认/抑制/关闭。
3. 必要时转事件（`iot_event_record`）并派发工单（`iot_event_work_order`）。
4. 工单执行并反馈，事件关闭形成闭环。
5. 报表聚合输出闭环指标。

补充口径：
- 2026-03-21 起，深部位移 `$dp` 子设备链路在属性落库后会发布风险判定事件，由 `alarm` 模块按 `risk_point_device` 绑定自动完成平台内闭环。
- 2026-04-02 起，自动闭环判级优先消费启用中的 `rule_definition`；仅当当前测点未命中可执行阈值策略时，才回退到 `iot.alarm.auto-closure.*` 默认阈值。
- 默认回退判级仍按毫米绝对值执行：蓝 `< 5`、黄 `[5, 10)`、橙 `[10, 20)`、红 `>= 20`。
- 黄色仅建告警；橙色、红色会继续自动转事件并派发工单；蓝色只更新风险点等级，不生成告警/事件。
- 自动闭环默认按 `deviceCode + metricIdentifier + level` 做 30 分钟冷却去重，风险点等级按全部绑定测点的最新值取最高等级后回写。
- 告警/事件留痕当前会写入 `policySource`、阈值文本、联动命中结果与预案命中结果，供风险运营与对象洞察统一回看。
- 自动闭环命中应急预案时，会优先筛选与“深部位移/位移/边坡/滑坡”及风险点上下文相关的启用预案；若只有无关通用预案，则留痕中的 `emergencyPlan` 为空，不再误挂无关预案。
- 已升级实例之间会额外使用 Redis 分布式锁抑制同一 `deviceCode + metricIdentifier + level` 的并发重复建单；若共享环境仍运行旧版 consumer，验收时可能出现新旧留痕并存，应先统一升级再判最终结果。

### 3.4 系统治理流程

1. `POST /api/auth/login` 登录，前端持久化 token。
2. 后端按用户角色返回动态菜单与按钮权限。
3. 前端请求自动附加 `Authorization: Bearer <token>`。
4. 业务页面按权限渲染按钮并调用对应接口。
5. 审计过滤器记录操作结果，失败和异常进入排障链路。

## 4. 业务语义边界（非交付判定）

### 4.1 当前稳定业务语义

- Phase 1~3 主链路继续作为设备接入与平台闭环的稳定业务语义。
- Phase 4 当前稳定业务语义覆盖实时监测、GIS 态势图、告警、事件、风险策略、报表分析、系统治理和系统内容治理。
- 产品定义中心当前语义定位为“产品主数据、接入契约与产品物模型治理入口”。
- 设备资产中心当前语义定位为“设备库存、父子拓扑、运行状态和未登记上报名单联查”。

### 4.2 当前已冻结并启动实现的下一阶段单一增强语义

- 产品定义中心下一子项：产品物模型设计器（独立任务卡见 [2026-03-24-product-model-designer-implementation-plan.md](./superpowers/plans/2026-03-24-product-model-designer-implementation-plan.md)）

冻结依据：
- `ProductWorkbenchView` 当前已经稳定承接产品主列表、接入契约、编辑删除与统一产品经营工作台；工作台内页固定收口为 `经营总览 / 物模型治理 / 关联设备 / 编辑治理` 四视图，并按“2026 A 方案年报中枢式 / 指挥舱刊物式”语法组织刊头、细线页签、年报展开页、治理标尺、设备清册与修订刊栏，不再沿用展签陈列式或政企简报式中轴对称表达。
- `DeviceWorkbenchView` 当前已经稳定承接设备资产中心台账、已登记/未登记双名单联查、父子拓扑、批量导入、设备更换，以及由 `DeviceDetailWorkbench` 统一承接的扁平详情工作台和等宽 `详情 / 编辑 / 更多` 操作列。
- 2026-03-25 已完成 `/products` 内物模型设计器的真实环境接口、数据库与页面复验，但该增强当前仍作为下一阶段单独推进，不与 Phase 4 已交付范围混算。
- 2026-04-01 起，设计器在同一抽屉内升级为“手动证据 + 自动证据 -> compare -> apply”治理链路，强调围绕当前产品把单设备样本、人工补录、运行期证据与正式模型基线放进同一套裁决视图，不把候选草案直接落库。
- 当前后续缺口已转为字段治理细化、统一 CI / 契约测试门禁和风险监测属性维护，而不是先继续扩远程维护或工单闭环。

### 4.3 当前明确不并行开启的设备中心增强语义

- 上下架审批
- 远程维护
- 维修工单联动

说明：
- `iot_product_model` 当前同时承接运行期消费与 `/products` 内嵌设计器治理；设计器首轮不新增顶层路由，也不新增独立按钮权限种子。
- 手动证据、自动证据与 compare/apply 当前都不新增 `iot_product_model_draft`、`iot_product_schema` 等平行草案表；候选只存在于接口返回体和前端抽屉状态中，显式 apply 后才进入正式契约。
- 上述“暂不并行开启”能力继续保留在业务词汇表中，但当前不计入“已交付范围”，并且不与产品物模型设计器任务卡并行实施。

## 5. 当前菜单结构与角色化工作台基线

### 5.1 已落地的一级工作台

当前前端导航与菜单种子已统一为五个一级工作台：

| 一级工作台 | 当前挂载功能 | 主要角色 |
|---|---|---|
| 接入智维 | `产品定义中心`、`设备资产中心`、`链路验证中心`、`异常观测台`、`链路追踪台`、`数据校验台` | 运维人员、开发人员 |
| 风险运营 | `实时监测台`、`GIS态势图`、`告警运营台`、`事件协同台`、`对象洞察台`、`运营分析中心` | 业务人员、管理人员 |
| 风险策略 | `风险对象中心`、`阈值策略`、`联动编排`、`应急预案库` | 管理人员、运维人员、开发人员 |
| 平台治理 | `组织架构`、`账号中心`、`角色权限`、`导航编排`、`区域版图`、`数据字典`、`通知编排`、`站内消息`、`帮助文档`、`审计中心` | 管理人员、超级管理员 |
| 质量工场 | `业务验收台`、`质量工场总览`、`研发工场总览`、`页面盘点台`、`场景模板台`、`计划编排台`、`交付打包台`、`执行中心`、`结果与基线中心` | 验收人员、产品、项目经理、开发人员、测试人员、超级管理员 |

补充口径：

- `接入智维总览` 当前定位为“中枢台”，通过 `资产底座 / 诊断排障` 两个业务页签承接最近使用恢复、推荐处理顺序和 6 个专台入口，不再在总览页重复子页正文。
- `产品定义中心`、`设备资产中心` 当前定位为“资产治理专台”：页面只保留轻量标题壳和主列表标题，主战区直接进入筛选、详情抽屉和治理动作；物模型设计器、关联设备、未登记资产与拓扑治理都回收到同一路由闭环。
- `平台治理` 下的 `账号中心 / 用户管理 / 角色权限` 当前已收口到同一条用户治理链：壳层账号中心直接消费 `/api/auth/me` 并允许当前登录用户维护基础资料；用户管理维护“用户 -> 主机构 -> 角色”关系；角色权限维护 `dataScopeType`，三者统一围绕 `sys_user.org_id + sys_user_role + sys_role.data_scope_type` 建模。2026-04-03 起，这套身份模型已继续落到第一批查询侧治理：`用户管理 / 组织管理 / 角色权限 / 风险对象中心` 会按当前登录人的租户、主机构和数据范围收口可见数据；`区域版图 / 数据字典 / 通知编排 / 帮助文档 / 审计中心` 当前已先按租户侧收口，其中帮助文档消费端会先按租户过滤，再叠加角色与页面路径权限。`GET /api/organization/tree` 保留查询口径，`GET /api/organization/writable-tree` 专门服务写入侧主机构选择。
- `风险策略` 下的 `风险对象中心`、`阈值策略`、`联动编排`、`应急预案库` 当前统一对齐 `产品定义中心` 的主列表合同：首屏空数据优先展示贴近表格结构的骨架屏，已有结果刷新时只在结果区叠加浅色蒙层，空态继续区分“无数据”与“筛选无结果”，分页统一使用 `10 / 20 / 50 / 100` 四档与 `total, sizes, prev, pager, next, jumper` 布局。
- `质量工场` 当前已拆分为“业务验收消费路径 + 研发自动化维护路径”两层语义：`/business-acceptance` 面向验收人员、产品和项目经理，只暴露 `环境 / 账号模板 / 模块范围` 三类轻配置，并在结果首屏直接回答“是否通过 / 哪些模块没过”；研发、测试与超管继续通过 `/rd-workbench`、`/automation-execution`、`/automation-results` 维护底层自动化资产、执行编排与证据台账。
- 2026-04-02 起，`风险策略` 与 `风险运营 / 接入智维` 的运行期口径开始收口到同一条后端执行链：`设备上报 -> risk_point_device 绑定 -> rule_definition 命中或 auto-closure 回退 -> 联动/预案命中留痕 -> 风险监测读侧投影`；后端新增 `/api/risk-governance/missing-bindings`、`/api/risk-governance/missing-policies` 用于把“已上报但未纳管对象”“已纳管但未配阈值策略”的治理缺口从接入侧导向风险策略侧。
- 2026-04-02 起，上述治理缺口不再只停留在后端接口：`对象洞察台` 当前会在正文区直接展示当前设备的 `待纳入风险对象 / 待配置阈值策略` 卡片，并提供跳转到 `风险对象中心 / 阈值策略` 的联动入口；`风险对象中心` 与 `阈值策略` 列表页则统一在 `notices + toolbar meta-items` 中显式展示全局待治理积压，不再把接入侧断点留在隐藏的后台接口里。
- `风险对象中心` 当前作为上述统一列表方案的窄例外：行内操作固定直出 `编辑 / 绑定设备 / 删除` 三项，不再把删除收口到 `更多` 菜单；风险点表单统一通过平台治理组织树选择“所属组织”，新增保存后自动生成风险点编号并避开已软删除历史编号，新增态不再展示编号输入，编辑态保留历史编号并只读回显 `创建人编号 / 更新人编号`；档案等级统一取自 `GET /api/dict/code/risk_point_level` 返回的启用项，运行态风险态势在列表中单列展示为红/橙/黄/蓝四色；新增、编辑、绑定、解绑、删除失败都必须给出显式反馈，避免用户误判动作未生效；解绑会逻辑删除该风险点下该设备的全部有效绑定，删除也统一走逻辑删除。2026-04-03 起，绑定设备弹窗已切到专用候选接口，候选清单会排除其他风险点已占用设备，并直接展示设备主档 `orgName` 归属结果。
- `风险对象中心` 当前已把 `risk_point.org_id / org_name` 与 `region_id / region_name` 共同作为风险点档案字段落库，`responsible_user` 继续绑定平台治理用户档案，用于按组织与区域收口风险点可见范围；2026-04-02 起新增/编辑抽屉不再预加载全量用户列表，负责人候选改为按当前可选组织的 `leader_user_id` 按需补齐，选择所属组织后默认带出该机构管理员与联系电话，联系电话仍允许人工改写；受限于当前未落机构成员关系表，负责人候选暂不扩展为机构全员列表。2026-04-03 起，所属区域选择也不再在页面挂载时预加载整棵 `/api/region/tree`，改为“打开抽屉后先加载根区域，展开节点再按 `GET /api/region/list?parentId=...` 懒加载子区域”的真实环境合同，避免大区域树把风险点页首屏拖慢。同日起，风险点列表、分页、详情、删除、绑定/解绑与绑定设备查询已按当前登录人的数据范围执行第一批查询侧收口，其中 `SELF` 只暴露本人负责或本人创建的风险点。
- `风险对象中心` 当前补充了一条离线治理入口约束：若外部 Excel 只提供 `风险点名称 + 设备编号`，而真实测点仍需等待设备上报综合治理，则先通过 `python3 scripts/export-risk-point-device-pending-bindings.py` 生成待治理 SQL，导入 `risk_point_device_pending_binding`；只有测点标识补齐后，才允许转正写入 `risk_point_device`。
- `风险对象中心` 当前已把 `risk_point.org_id / org_name` 与 `region_id / region_name` 共同作为风险点档案字段落库，`responsible_user` 继续绑定平台治理用户档案，用于按组织与区域收口风险点可见范围；2026-04-02 起新增/编辑抽屉不再预加载全量用户列表，负责人候选改为按当前可选组织的 `leader_user_id` 按需补齐，选择所属组织后默认带出该机构管理员与联系电话，联系电话仍允许人工改写；受限于当前未落机构成员关系表，负责人候选暂不扩展为机构全员列表。同日起，风险点列表、分页、详情、删除、绑定/解绑与绑定设备查询已按当前登录人的数据范围执行第一批查询侧收口，其中 `SELF` 只暴露本人负责或本人创建的风险点。
- `风险对象中心` 自 `2026-04-03` 起新增 pending 治理写侧闭环：围绕同一路由 `/risk-point`，后端当前同时提供 `GET /api/risk-point/pending-bindings`、`GET /api/risk-point/pending-bindings/{pendingId}/candidates`、`POST /api/risk-point/pending-bindings/{pendingId}/promote` 与 `POST /api/risk-point/pending-bindings/{pendingId}/ignore`。前端通过“待治理转正”抽屉分页查看某风险点下的待治理绑定、读取系统推荐候选测点与既往转正历史，并允许人工勾选一个或多个候选测点写入正式 `risk_point_device`。`risk_point_device_pending_binding` 继续承担导入/治理中转，`risk_point_device_pending_promotion` 负责单测点转正留痕；运行态真相仍以 `risk_point_device` 为准，不再写入占位测点。
- `2026-04-03` 起，pending 候选不再按“设备上报里出现过的所有字段”直接枚举，而是按监测业务规范收口重点测点：系统会综合设备名称、风险点上下文和运行证据，只保留对当前监测类型有业务意义的规范字段；`battery_dump_energy / signal_4g / lat / lon / sw_version` 等设备状态参数默认不进入正式候选。
- 当运行期证据同时出现规范字段与厂商原始字段时，pending 候选当前统一以规范字段名作为 `metricIdentifier`，并在 `reasonSummary` 中保留原始证据别名，例如把 `L1_SW_1.dispsX` 收口展示为 `dispsX`。转正写侧会复用同一套规则，若人工提交状态参数或不在规范白名单内的字段，则直接返回 `INVALID_METRIC`，不写入正式 `risk_point_device`。
- `链路验证中心`、`链路追踪台`、`数据校验台` 当前定位为“诊断专台”：页面顶部只保留轻量标题壳；跨页联动动作优先下沉到正文判断区、行内操作或详情抽屉，主战区通过真实业务页签组织模拟验证/复盘、链路追踪/失败归档、设备校验/原始响应。
- `异常观测台` 当前回到单主列表页，只保留异常台账、筛选和详情/回跳动作，不再保留聚合视图、页头跳转菜单或说明型工作区。
- `接入智维` 子页若存在两个及以上并列内容域，当前只在“确有两个以上真实业务视图”时通过同页 Tab 工作区承接；不再新增页面私有 pill、重复说明卡或二级轻路由去表达假切换。
- `接入智维` 首批跨页排查顺序当前收口为“总览判断 -> 链路验证/异常观测/链路追踪/数据校验/失败归档 -> 产品定义中心/设备资产中心”；总览负责判断入口，诊断页负责给出证据，治理页负责承接修正，不再互相复制正文。

### 5.2 结构调整结果

本轮实际完成的结构调整如下：

1. `对象洞察台` 已从接入侧移入 `风险运营`，用于承接监测对象属性、日志与研判线索。
2. `对象洞察台` 当前优先使用 `risk-monitoring` 实时详情作为风险运营口径：风险点档案等级、当前风险态势、在线状态、监测对象档案、关键测点和趋势预览统一来自 `/api/risk-monitoring/realtime/{bindingId}`；其中 `riskPointLevel` 对应档案口径 `level_1 / level_2 / level_3`，`currentRiskLevel` 对应运行态红/橙/黄/蓝四色，历史 `riskLevel` 仅作为兼容镜像保留。若页面只传入 `deviceCode`，则先经 `/api/risk-monitoring/realtime/list` 解析监测绑定，再选定主绑定进入详情。`GET /api/device/code/{deviceCode}`、`GET /api/device/{deviceCode}/properties`、`GET /api/device/{deviceCode}/message-logs` 继续承担设备补充档案、属性快照和消息日志数来源；当设备尚未纳入风险监测绑定时，页面会退化为“设备上报分析视图”，继续展示设备档案、属性快照、消息日志和基础研判，其中当前风险态势仍保持未绑定状态，但“属性趋势预览”会回退为最近消息日志中数值字段的折线趋势。
3. `对象洞察台` 当前正文固定收口为“当前风险态势 / 在线状态 / 属性快照数 / 消息日志数”四项概览，以及 `基础档案`、`研判依据`、`属性趋势预览`、`关键监测指标`、`设备属性快照`、`风险分析草稿` 六块正文；`当前建议动作`、`一线建议`、`运维建议`、`研发建议` 与独立“消息日志与审计回看”卡不再保留。
4. `对象洞察台` 当前会并行查询 `/api/risk-governance/missing-bindings` 与 `/api/risk-governance/missing-policies`；若当前设备存在治理缺口，则正文区追加 `治理缺口` 卡片，明确提示“已有上报但未纳管”或“已纳管但未配阈值策略”，并把后续动作直接导向 `风险对象中心` 或 `阈值策略`。
4. `实时监测台`、`GIS态势图` 已于 2026-03-24 完成真实环境复验，正式收口到 `风险运营`，不再保留灰度标签或独立“风险增强”口径。
5. `质量工场` 已从早期单页 `自动化工场` 演进为 `/quality-workbench` 总览 + `/business-acceptance` 业务验收台 + `/rd-workbench` 研发总览 + `/rd-automation-inventory`、`/rd-automation-templates`、`/rd-automation-plans`、`/rd-automation-handoff` 四个研发专项页 + `/automation-execution`、`/automation-results` 两个共享中心；业务验收台首屏只保留 `环境 / 账号模板 / 模块范围` 三类轻配置，并在结果页优先展示“是否通过 / 哪些模块没过 / 失败步骤、接口、页面动作”；`/automation-results` 继续承担底层运行台账与证据中心，并支持携带 `runId` 直接落到同一次运行。`/automation-assets` 与 `/automation-test` 当前仅保留兼容入口并默认落到研发工场总览，避免工程工具再次回流到同页堆叠。
6. `设备接入 / 预警处置 / 系统治理` 的旧命名已分别升级为 `接入智维 / 风险运营 / 平台治理`，并新增 `风险策略 / 质量工场` 两个顶层工作台。

### 5.3 角色首页与默认落点

后端 `/api/auth/me` 返回的 `homePath` 已按角色收口为工作台首页；当前前端也已补齐同口径 fallback 规则，在历史菜单数据缺失或 `homePath` 为空时，仍按同一套角色策略回落到对应工作台总览。默认规则如下：

| 角色 | 默认首页 | 说明 |
|---|---|---|
| 业务人员 | `/risk-disposal` | 默认先进入风险运营总览 |
| 管理人员 | `/risk-disposal` | 先看风险运营，再进入风险策略和平台治理 |
| 运维人员 | `/device-access` | 默认先进入接入智维总览 |
| 开发人员 | `/device-access` | 默认先进入接入智维，总览后继续到风险策略和质量工场 |
| 超级管理员 | `/system-management` | 默认先进入平台治理总览 |

若目标工作台根菜单未授权，后端会退回到首个可访问业务页面，避免空首页。

### 5.4 导航与搜索工作方式

- 一级导航、二级导航、分组首页、路由 `title/description`、角色默认落点当前统一由前端共享 workspace schema 派生，避免 `AppShell`、分组首页与路由表各自维护一套菜单语义。
- 顶部搜索已升级为全局命令面板，支持 `Ctrl/Cmd + K` 快捷唤起，按当前账号可见菜单在五个工作台内检索功能入口。
- 右上角 `通知中心 / 帮助中心` 已升级为角色化壳层入口：通知中心按当前角色、工作台、最近操作和失败记录聚合 `系统事件 / 业务事件 / 错误事件`；帮助中心按当前菜单权限筛选 `业务类 / 技术类 / FAQ` 资料。
- 2026-04-02 起，右上角账号菜单统一只保留 `账号中心 / 修改密码 / 退出登录`；原 `实名认证`、`登录方式` 独立静态入口已并入账号中心抽屉，不再维持第二套壳层说明页。
- 2026-03-21 起，`spring-boot-iot-system` 已提供与壳层一致的后端支撑；2026-03-21 晚间起，前端壳层已优先消费站内消息与帮助文档接口，本地内容构造仅在接口不可用时作为兜底，分类口径继续保持 `系统事件 / 业务事件 / 错误事件` 与 `业务类 / 技术类 / FAQ`。
- 壳层摘要弹层当前只展示少量分组卡片，并在底部统一提供“查看更多”；通知摘要额外提供“全部已读”，帮助摘要继续只做消费入口，不新增独立菜单或私有页面模块。
- 账号中心当前收口为单一抽屉，固定展示 `基础资料 / 机构与角色 / 安全信息 / 实名资料` 四段；基础资料直接调用 `PUT /api/user/profile` 维护当前登录人的昵称、真实姓名、手机号、邮箱，机构与角色、安全信息统一复用 `/api/auth/me` 返回的租户、主机构、角色、登录方式、最近登录和数据范围摘要。
- 通知消费闭环当前已收口为“摘要 -> 列表抽屉 -> 详情抽屉 -> 显式已读”：打开摘要或详情都不会自动清空未读，只有点击“标记已读”或“全部已读”时才更新未读徽标。
- 2026-03-22 起，通知中心生产侧也已收口到统一内部发布服务：`system_error`、事件派工、工单接收/开始/完成都会自动生成站内消息，并继续由全局壳层消费，不新增独立通知页面。
- 通知中心当前固定来源类型为 `manual / system_error / event_dispatch / work_order / governance`；治理侧可筛选来源、查看统计，并对系统自动消息执行“查看或停用”，不再直接改正文或删除。
- 2026-03-22 深夜起，通知中心已新增“高优未读阈值后桥接通知渠道”闭环：
  - 只对 `high / critical` 站内消息生效。
  - 超过阈值仍未读时，后端按调度扫描并复用 `/api/system/channel` 既有渠道配置桥接到外部群通知，不新增新的多渠道通知产品页。
  - 桥接仍按渠道 `config.scenes` 过滤，场景名固定为 `in_app_unread_bridge`。
- 2026-03-22 晚间起，平台治理 `/in-app-message` 已在原消息治理列表下方补齐“桥接效果运营”专区：
  - 不新增独立菜单或路由，继续留在同页折叠区内完成桥接统计和日志治理。
  - 管理侧可查看桥接记录数、桥接成功数、待重试数、桥接成功率、渠道/来源分布，以及桥接日志分页。
  - 单条桥接记录可打开独立桥接详情抽屉，同时查看消息原文和按尝试序号倒序排列的逐次尝试明细。
- 帮助消费闭环当前已收口为“摘要 -> 列表抽屉 -> 详情抽屉”：列表抽屉支持关键字搜索、服务端分页、当前页相关资料优先排序，以及标题/摘要/关键词/正文四处安全高亮。
- 2026-03-21 夜间起，平台治理工作台已新增 `/in-app-message` 与 `/help-doc` 前端编排页，管理/超管可直接维护消息分类、投放范围、角色/用户定向、帮助分类、可见角色和关联页面路径。
- 2026-04-01 起，风险运营下的 `实时监测台 / GIS态势图 / 告警运营台 / 事件协同台`，以及平台治理下的 `组织架构 / 账号中心 / 角色权限 / 导航编排 / 区域版图 / 数据字典 / 通知编排 / 帮助文档管理` 已统一复用 `产品定义中心` 的列表交互合同：不再调整页面布局层级，只统一结果区骨架屏、已有结果后的局部刷新蒙层、空态分流、`10 / 20 / 50 / 100` 分页与请求乱序保护；其中 `GIS态势图` 继续保留“已定位 / 未定位”双分区，`组织架构 / 导航编排 / 区域版图` 继续保留“默认懒加载树 / 搜索后扁平分页”双模式。
- 原“最近访问标签栏”已移除，功能页顶部仅保留面包屑；页面定位继续由一级工作台、左侧二级菜单和命令面板承担。

### 5.5 当前角色可见范围原则

| 角色 | 当前默认授权范围 |
|---|---|
| 业务人员 | 风险运营主链路 + 业务验收台 + 产品定义中心/设备资产中心查看导出，用于确认产品和设备库存是否已建档，并按交付清单发起一键业务验收 |
| 管理人员 | 风险运营 + 风险策略 + 平台治理主体能力 + 业务验收台 + 产品定义中心/设备资产中心增删改、批量导入、设备更换、导出，并具备通知编排、站内消息、帮助文档及用户/角色部分按钮权限 |
| 运维人员 | 接入智维全链路 + 风险运营精选页 + 风险策略中的对象/阈值配置，默认具备产品定义中心/设备资产中心增删改、批量导入、设备更换、导出 |
| 开发人员 | 接入智维全链路 + 风险运营排障页 + 风险策略全链路 + 质量工场，默认具备产品定义中心/设备资产中心增删改、批量导入、设备更换、导出 |
| 超级管理员 | 全部菜单、全部按钮权限 |

## 6. 待确认与补充说明

- `2026-03-24` 已基于真实环境复验确认 `实时监测台`、`GIS态势图` 正式并入 `风险运营`，不再保留灰度标签。
- `【待确认 DOC-Q-001】` 告警自动转事件触发策略的正式业务规则（目前能看到数据结构和处置接口，缺少完整规则说明）。
- `【根据代码推断】` 当前角色首页、默认路由、角色关注路径和命令面板入口已统一收口到前端共享 workspace schema；后续仍需继续把这套规则与数据库菜单种子、后端 `homePath` 生成策略保持同源。
- 多租户跨组织数据权限模型已统一收口到 [13-数据权限与多租户模型.md](./13-数据权限与多租户模型.md)；当前仍未关单的事项继续在 [08-变更记录与技术债清单.md](./08-变更记录与技术债清单.md) 中维护。
