# 产品经营工作台首长简报式视觉进化设计

> 日期：2026-03-31
> 范围：`spring-boot-iot-ui`
> 主题：在既有扁平化产品经营工作台基础上，继续进化为“首长简报式”的政企经营工作台
> 状态：设计已确认，待进入实现

## 1. 背景

`产品定义中心` 的统一产品经营工作台已经完成第一轮扁平化重构，解决了以下问题：

- `详情 / 物模型治理 / 关联设备 / 编辑治理` 已统一进入一个工作台抽屉
- 行内操作已收口为 `进入工作台 + 删除`
- 内页不再是旧版多套对象抽屉并行存在

但当前效果仍主要停留在“结构被压平”，还没有形成足够稳定的政企经营气场。用户进一步指出：

- 页面虽然统一了，但还不够大气
- 标题、留白、区块关系和阅读路径还不够像正式经营简报
- 各内页之间仍有轻微“组件拼装感”，缺少一套更强的上位排版秩序

因此，这一轮不再解决功能问题，而是继续提升视觉语言与版式气场。

## 2. 目标

- 在现有扁平化骨架上，升级为“首长简报式”的政企经营工作台。
- 强化标题层级、章节编排和留白节奏，让页面更稳、更克制、更有汇报气场。
- 让四个内页共享同一套“定调头部 + 章节主板块 + 节内主舞台”的阅读秩序。
- 保留统一工作台、统一页签、统一组件基线，不回退成私有大屏或营销风设计。

## 3. 非目标

- 不新增路由，不拆出独立详情页。
- 不新增后端接口或聚合字段。
- 不引入新的品牌主色体系，不做蓝紫科技风、深色驾驶舱风或营销展陈风。
- 不回流厚 hero、重渐变、大面积说明墙和第二套工作台骨架。

## 4. 用户已确认方向

本轮已确认：

- 主轴：`A 排版气场`
- 气质：`政企经营简报式`
- 落地方案：`A. 首长简报式`
- 最新范围：`产品经营工作台头部 + 经营总览 / 物模型治理 / 关联设备 / 编辑治理四个页签全部统一为 A 语法`

解释固定为：

- 重点提升“大标题压场、章节主次、留白秩序、阅读威压感”
- 不追求花哨视觉，不靠色块堆砌气场
- 仍以统一工作台为容器，但内部要更像正式经营汇报材料

范围固定解释为：

- 不是只优化某一个页签，而是把整套产品经营工作台收口为同一种“首长简报式”阅读秩序
- 头部、真页签、章节标题、摘要带、台账/表单/治理正文必须说同一种视觉语言
- 改造只允许作用于前端结构、文案层级、留白秩序、视觉 token 使用和测试锚点，不得借机改路由、接口、抽屉行为和数据契约

## 5. 总体设计原则

### 5.1 标题先定调

每个内页都必须有足够明确的“先定调，再读数，再落治理”结构：

- 一级：工作台头部的产品名与状态判断
- 二级：当前视图的章节标题
- 三级：节内的主舞台标题或台账标题

标题职责必须明确，避免当前“标题很多，但没有主次压差”的问题。

### 5.2 留白作为秩序，不是空

页面要通过统一的上下间距、区块内边距和列间距来建立稳定感：

- 头部与正文拉开更明确的呼吸距离
- 大区块之间用更完整的段落间距
- 节内卡片数量减少，但每块更完整、更有落点

### 5.3 章节化，而不是卡片化

这轮不继续增加卡片，而是减少“同权重卡片平铺”，改为“章节主板块 + 辅助信息”：

- 每一屏只保留一个主舞台
- 辅助指标只承担陪衬，不抢标题
- 治理提示要更像结论和批注，不像漂浮说明块

### 5.4 协调的流线

从头部到正文的阅读顺序固定为：

1. 产品身份与当前状态
2. 当前视图的核心章节标题
3. 首屏唯一主舞台
4. 支撑判断的辅助信息
5. 治理或台账主区

这样能把“工作台”从零散阅读变成有顺序的经营汇报阅读。

## 6. 统一工作台壳层

### 6.1 头部

统一工作台头部继续保留：

- 产品名称
- 产品 Key
- 状态 badge
- `productKey / 协议 / 节点 / 数据格式`
- 四个真实页签

但本轮要升级为：

- 标题字号更有压场感
- `产品经营工作台` 眉题更轻，但更像章节索引
- 状态 badge 更像“当前判断”，而不是普通标签
- meta chips 与 tabs 的节奏更规整，形成“简报页眉”感

### 6.2 视图壳层

统一工作台正文容器继续保留一个总 `view-shell`，但要把：

- 边框更轻
- 圆角更稳
- 背景更干净
- 页签与头部之间的节奏更顺

目标是让用户先感受到“这是一个完整的经营工作台页面”，而不是抽屉里嵌了 4 个组件。

### 6.3 固定阅读路径

工作台头部到四个页签正文的阅读路径本轮固定为：

1. 产品身份与状态判断
2. 当前页签的章节标题与一句导语
3. 当前页签首屏唯一主舞台
4. 辅助摘要带或治理提示
5. 台账、表单或治理正文

这条路径对四个页签全部生效。允许板块内容不同，但不允许再回流为“多个同权重卡片各说各话”的拼装页。

## 7. 各视图落地

### 7.1 经营总览

总览页仍是整套工作台的主舞台，这轮要最先“立住气场”。

结构固定为：

1. 章节头：`设备规模与经营判断`
2. 主舞台：只保留一个大数与结论短句
3. 辅助指标列：在线设备、30 日活跃、平均在线时长
4. 经营判断板块：趋势摘要、接入提示
5. 契约与档案板块
6. 维护治理板块

精修重点：

- 主数字更像汇报中的“核心经营数据”
- 辅助指标退到次级阅读层
- “趋势摘要”“接入提示”“当前建议”改成更像经营判断结论
- 契约和档案不再显得像散列字段列表，而要更像附录摘要

行为边界：

- 不改现有指标数据来源和字段口径
- 不新增概览接口或额外统计请求
- 继续保留当前 `ProductDetailWorkbench` 的主数据与趋势数据展示范围，只调整呈现主次与文案压缩策略

### 7.2 物模型治理

物模型页需要从“治理组件页”提升为“治理简报页”。

结构固定为：

1. 章节头：`物模型治理`
2. 摘要条：真实证据概览、属性候选、待人工确认、正式模型
3. 主工作区：左目录、中主列表、右治理轨道

精修重点：

- 模式切换更像正式页签，不像切换按钮
- 摘要条更像概览条目，不像重复摘要卡
- 中间主列表更像“当前正文”，左右两侧退为辅区
- `进入完整治理` 按钮要成为清晰但克制的主动作

行为边界：

- 继续保留 `候选提炼 / 正式模型` 双模式与完整治理抽屉
- 不新增一级路由，不改变现有模式切换逻辑
- 继续沿用真实证据、候选目录、主列表和治理轨道的数据来源，只重构章节化表达

### 7.3 关联设备

关联设备页要从“概览 + 表格”提升为“先定调，再核账”的台账简报。

结构固定为：

1. 章节头：`设备运行概览`
2. 指标带：设备总数、在线设备、离线设备、在线比例
3. 台账主区：设备列表

精修重点：

- 指标带变成更统一的摘要横带，不再像几块轻卡片散落
- 台账标题与表格成为真正的主体
- 表头、空态和表格包裹层要更稳，不要显得轻飘

行为边界：

- 不新增设备页签内的二级切换，不新增桌面卡片视图
- 继续保留当前设备表格字段、查看动作和空态语义
- 加载、错误、空态仍在同一页签内表达，不拆成第二套状态壳

### 7.4 编辑治理

编辑页要从“编辑表单页”提升为“变更申请页”的气质。

结构固定为：

1. 章节头：`编辑治理`
2. 档案摘要条：产品 Key、协议编码、节点与状态
3. 内联治理提示
4. 表单主区：基础档案、接入基线、补充说明

精修重点：

- 章节头必须先定调“当前在做什么变更”
- 摘要条更像“变更上下文”
- 表单分区标题更正式，弱化普通表单感
- 保存区保持克制，不做大面积强视觉操作条

行为边界：

- 不改变新增/编辑表单字段、校验规则和提交行为
- 继续保留当前内联治理提示和底部操作区
- 只允许把“编辑页”提升为“变更申请页”的视觉气质，不允许引入新的审批流语义或额外步骤

## 8. 交互与异常状态合同

本轮不改交互协议，但必须显式锁定以下状态合同，避免视觉改造顺手改行为：

- 工作台仍然是同一个 `StandardDetailDrawer`，尺寸与开闭契约不变
- 四个页签仍然在同一抽屉内切换，并继续保活已访问视图
- `经营总览 / 物模型治理 / 关联设备 / 编辑治理` 的加载、错误、空态继续在各自页签内表达，不新增全局遮罩态或独立错误页
- `关联设备` 的 `loading / error / empty` 仍使用同一组件内状态切换；只是样式语法更统一
- `物模型治理` 在候选接口不可用时继续降级到正式模型模式，不因为视觉改造破坏现有回退链路
- `编辑治理` 的取消/保存位置和语义保持不变，只调整排版和主次

## 9. 测试与验证策略

- 继续使用组件测试锁定 DOM 结构与关键文案锚点
- 对五个产品工作台组件补充“首长简报式”结构锚点断言
- 保持 `/products` 视图测试通过，避免工作台容器、页签默认视图或行内入口回退
- 继续执行：
  - 组件测试
  - `ProductWorkbenchView` 视图测试
  - `component:guard`
  - `list:guard`
  - `style:guard`
  - `build`

新增测试关注点：

- `ProductBusinessWorkbenchDrawer`：头部必须继续存在，但断言要从“扁平化头部”升级为“简报式头部”锚点
- `ProductDetailWorkbench`：断言主舞台、判断区和附录区仍按简报式章节存在
- `ProductModelDesignerWorkspace`：断言章节头、摘要条、主工作区/侧轨道结构保持稳定
- `ProductDeviceListWorkspace`：断言“章节头 + 摘要横带 + 台账主区”替代旧的轻卡片概览感
- `ProductEditWorkspace`：断言“章节头 + 变更上下文条 + 表单主区”的结构稳定

## 10. 文档同步要求

本轮属于前端页面结构和设计语法演进，必须原位更新：

- `docs/06-前端开发与CSS规范.md`
- `docs/08-变更记录与技术债清单.md`
- `docs/15-前端优化与治理计划.md`

需要补充的规则包括：

- 产品经营工作台第二轮“首长简报式”语法
- 统一工作台头部与各内页的标题主次规则
- 防止再次回流成普通卡片拼装页、说明墙或厚 hero
