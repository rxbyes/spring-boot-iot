# 前端工作区与监测预警平台首页

## 目标
为当前 `spring-boot-iot` Phase 1 / Phase 2 后端能力提供一套独立的 Vue 3 前端，覆盖：
- 产品管理联调
- 设备管理联调
- HTTP 模拟设备上报
- 设备最新属性查询
- 设备消息日志查询

同时把现有“研发调试台”逐步改造成更接近商业产品的驾驶舱，服务：
- 一线工作人员
- 运维维护人员
- 开发与实施人员

并为后续这些方向预留前端扩展面：
- 点位数据图表
- 数字孪生
- 网关与子设备拓扑
- 规则 / 告警 / OTA 操作面板

## 目录
前端工作区位于：

`spring-boot-iot-ui`

说明：
- 该目录不加入 Maven reactor
- 采用 Vue 3 + Vite 风格工程结构
- 公共壳层采用“顶部双层导航 + 左侧业务菜单 + 右侧内容区”的产品化结构，围绕监测预警业务分区组织页面
- 公共壳层视觉已切换为浅色产品控制台风格，品牌统一为 `监测预警平台`
- 当前已经接入 `Element Plus` 组件体系和 `ECharts` 图表能力

## 页面结构

### 公共壳层（2026-03）
- 顶部第一行：品牌入口、页面搜索、平台状态摘要
- 顶部第二行：一级业务分区导航（平台首页 / 设备接入 / 预警处置 / 系统治理）
- 左侧：当前一级分区下的二级菜单，显示页面名称与用途说明
- 右侧：页面上下文、登录态操作和业务内容；平台首页不再展示最近访问标签，登录页聚焦账号/手机登录与扫码入口
- 移动端：侧边菜单切换为抽屉式展开

### 视觉一致性优化（2026-03）
- 全局 Token、Element Plus 组件覆盖和通用卡片样式已切换为浅色控制台体系
- 已优先完成驾驶舱、产品工作台、设备工作台、上报工作台、监测对象工作台等核心页面的深色面板替换
- 已完成风险监测增强页（`/risk-monitoring`、`/risk-monitoring-gis`）及监测详情抽屉的浅色统一
- 已统一驾驶舱与风险监测相关 ECharts 的 tooltip、坐标轴和主题色，避免深色图表残留
- 公共壳层（顶部条、一级导航、侧边栏、内容工具栏）已按控制台化风格进行间距与层级重排：减少装饰性渐变、统一边框半径与分组间距，提升信息密度和稳定性
- 公共列表页模板已统一：系统管理与风险处置页面共享“筛选条 + 列表容器 + 分页条”样式基线，筛选区和表格区间距对齐同一节奏
- 2026-03-16 已补齐设备运维中心与监测对象工作台的状态条、建议卡片浅色化，移除黑底白字残留并统一为阿里云浅色控制台风格
- 2026-03-16 已补齐产品模板中心与接入验证中心的按钮层级、预览区与卡片强调样式，统一为浅色产品控制台视觉
- 2026-03-16 已补齐告警中心与事件处置的筛选区、统计卡片、操作按钮与分页区样式，统一到浅色控制台规范
- 2026-03-17 已补齐告警中心与事件处置弹窗（详情/派发/关闭）的标题区、内容区与底部按钮区样式，保持页面与弹窗一致体验
- 2026-03-17 已补齐系统管理（组织/用户/角色/区域/字典/通知渠道）弹窗样式，统一弹窗头部、内容区与主次按钮规范
- 2026-03-17 已补齐系统管理页全局骨架样式（筛选区、表格区、分页区）并接入 `sys-mgmt-view`，同时完成组织/区域树筛选、字典/渠道筛选与字典项新增编辑交互补全
- 2026-03-17 已补齐告警中心、事件处置、组织、用户、角色、区域、字典、通知渠道、审计日志 9 个列表页的统一表格操作栏，支持“已选项计数 / 清空选中 / 刷新列表”
- 2026-03-17 已补齐上述列表页“导出选中”CSV 能力（前端本地导出，无需后端新增接口）；字典项弹窗列表也同步支持
- 2026-03-17 已补齐“导出选中”CSV 的中文列头与状态文案映射（避免导出字段名与码值直出）
- 2026-03-17 已补齐“导出当前结果”CSV 能力（基于当前列表加载结果导出），与“导出选中”形成双导出入口
- 2026-03-17 已补齐“导出列设置”能力：支持列勾选、列顺序调整、恢复默认，并按页面在本地持久化导出配置
- 2026-03-17 已补齐“导出模板”能力：在导出列设置中提供默认/运维/管理模板，一键切换导出列组合
- 2026-03-17 已补齐“自定义导出模板”管理能力：支持保存为模板、删除模板，按页面本地持久化
- 2026-03-17 已补齐“模板JSON导入/导出”能力，支持跨账号或跨环境共享导出模板
- 2026-03-17 已补齐模板重命名与分组标签能力，支持按分组筛选模板并管理自定义模板命名
- 2026-03-17 已补齐模板导入冲突策略（覆盖/跳过/重命名），并明确导入模板不会直接改动当前导出列勾选状态
- 2026-03-17 已补齐模板搜索与最近使用置顶能力，支持按名称快速定位模板并提升高频模板操作效率
- 2026-03-17 已补齐模板导入预览确认能力：导入前展示新增/覆盖/重命名/跳过摘要并二次确认
- 2026-03-17 已补齐模板导入预览明细能力：支持查看新增/覆盖/重命名/跳过完整列表，并可在明细弹窗中直接确认导入
- 2026-03-17 已补齐风险点管理“绑定设备”闭环：打开弹窗自动加载设备列表，选择设备后联动加载测点，并通过 `/api/risk-point/bind-device` 完成真实绑定提交
- 2026-03-17 已补齐风险点管理页面骨架统一：切换到 `sys-mgmt-view` 与 `sys-dialog` 样式体系，并补齐列表“已选计数 / 清空选中 / 刷新列表”操作条
- 2026-03-17 已补齐阈值规则、联动规则、应急预案三页骨架统一：切换到 `sys-mgmt-view` 与 `sys-dialog` 样式体系，补齐列表操作条并修复状态筛选 `0/1` 传参准确性
- 2026-03-17 已与后端同步分页接口：风险点、阈值规则、联动规则、应急预案列表默认改走 `/page` 服务端分页，保留 `/list` 兼容口径

### 1. 监测预警平台首页
- 首页已重构为“数据看板驾驶舱 + 事务工作台入口”双层结构。
- 首屏优先呈现指标看板，不再用工作台内容占据首页主体。
- 看板聚焦关键指标、风险队列、角色待办和处置效率，工作台入口下沉为执行层。
- 当前作为公开首页，未登录状态也可直接访问。

**驾驶舱改造（2026-03）**：
- Hero 区：强调“风险数据驾驶舱”定位，展示当前统计口径与平台时间。
- 角色视角：支持四类角色 preset：
  - 一线工作人员
  - 运维人员
  - 管理人员
  - 研发人员
- KPI 看板：按角色切换展示不同关键指标（响应时长、在线率、闭环效率、链路健康等）。
- KPI 看板：优先基于 `/api/report/risk-trend`、`/api/report/alarm-statistics`、`/api/report/event-closure`、`/api/report/device-health` 的真实聚合数据计算；接口不可用时自动回退稳定口径。
- 队列看板：按风险优先级展示告警、事件、规则、稳定性等关键队列。
- 待办看板：按角色输出可执行待办，首页直接驱动处置动作。
- 事务工作台：保留告警中心、事件处置、风险点、报表、设备、接入验证等入口，作为后续执行窗口。
- 首页主视图继续采用稳定静态口径与本地 activity 记录，不依赖旧 `/api/cockpit/*` 演示接口。

### 2. 产品模板中心
- 对接 `POST /device/product/add`
- 对接 `GET /device/product/{id}`
- 用于维护产品建模模板，支撑设备归属和协议引用

### 3. 设备运维中心
- 对接 `POST /device/add`
- 对接 `GET /device/{id}`
- 对接 `GET /device/code/{deviceCode}`
- 用于验证设备建档、状态字段和最近上报时间
- 后续承接远程控制、阈值管理与运维操作
- 页面已按商业产品方式重组为：
  - 设备健康状态
  - 认证基线核查
  - 运维动作建议
  - 远程运维预留位
  - 研发排障提示

### 4. 接入验证中心
- 对接 `POST /message/http/report`
- 支持快速切换属性、状态、事件三类示例 payload
- 自动推导推荐 topic
- 提供 curl 预览，方便与文档或终端联调命令对照
- 后续扩展 MQTT / TCP 接入联调

### 5. 监测对象工作台
- 对接 `GET /device/code/{deviceCode}`
- 对接 `GET /device/{deviceCode}/properties`
- 对接 `GET /device/{deviceCode}/message-logs`
- 聚合设备详情、属性快照和消息日志，作为一线风险研判核心工作页
- 前端会基于现有数据做轻量风险分级、动作建议和报告草稿生成
- 页面结构按“一线人员 / 运维人员 / 研发人员”三类角色重组信息
- 当前已增加基于最近消息日志的 `ECharts` 趋势图，后续可直接衔接时序库查询

### 6. 数据完整性校验
- 对接 `GET /device/{deviceCode}/file-snapshots`
- 对接 `GET /device/{deviceCode}/firmware-aggregates`
- 用于验证表 C.3 文件快照和表 C.4 固件分包聚合是否已经被后端消费
- 展示 Redis 快照、分包进度、重组长度和 MD5 校验结果

### 7. 未来演进蓝图
- 展示点位图表、数字孪生、拓扑与规则的预留设计位
- 说明当前页面结构与未来模块的衔接方式
- 作为商业产品路线图的前端展示位

## 运行方式
1. 启动后端：`mvn -pl spring-boot-iot-admin spring-boot:run`
2. 进入前端目录：`cd spring-boot-iot-ui`
3. 安装依赖：`npm install`
4. 启动开发环境：`npm run dev`

默认行为：
- 若 `VITE_API_BASE_URL` 为空，则前端走 Vite 本地代理
- 默认代理目标为 `http://127.0.0.1:9999`（可通过 `VITE_PROXY_TARGET` 覆盖）
- 开发环境推荐保持 `VITE_API_BASE_URL` 为空，这样浏览器请求会走相对路径，避免 `http://localhost:5173` 直连 `http://127.0.0.1:9999` 时触发 CORS
- 登录页已移除“接入设置”输入框，当前采用环境变量和 Vite 代理统一管理联调地址
- 后端当前已提供最小 CORS 支持，默认允许 `http://localhost:*` 与 `http://127.0.0.1:*` 作为浏览器开发来源
- `/` 可作为公开首页直接访问；其他受保护页面在未登录时会跳转到 `/login`

可通过 `spring-boot-iot-ui/.env.example` 参考配置：
- `VITE_API_BASE_URL`
- `VITE_PROXY_TARGET`

当前 Node 基线：
- 前端开发与构建统一以 Node 24 为基线
- 推荐直接使用 `spring-boot-iot-ui/.nvmrc`
- 若终端仍命中旧版 Node，需要先切换到 Node 24 再执行安装、开发和构建命令

## 当前设计原则
- 不改动后端主链路
- 前后端保持独立目录和清晰职责
- 当前首页优先向商业产品形态靠拢，但不牺牲联调效率
- 所有未来功能入口都基于已有数据面板扩展，不推翻现有结构
- 文件 / 固件调试台优先服务 MQTT C.3 / C.4 联调，不提前扩展成完整 OTA 运维面板
- 关键查询状态逐步同步到 URL，方便分享调试链接和恢复现场

## 已知说明
- 当前前端基于 Node 24 + 新版 Vite 工具链运行
- 若你的 shell / IDE 内置终端仍命中旧版 Node，可能会出现 `Cannot use import statement outside a module` 之类的错误；先切换到 Node 24 再启动
- 当前前端主要服务开发和联调场景，后续如需嵌入 `spring-boot-iot-admin` 静态资源，可再做打包接入
- `/` 当前作为公开产品首页；其他受保护页面在未登录时会跳转到 `/login`
