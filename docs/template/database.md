请先打开 `docs/09-GPT接管提示模板.md`，严格按其中的 `6.6 数据库 / SQL / 初始化脚本专用模板` 执行。

本次数据库任务：
【填写任务，例如“补字段 / 修初始化脚本 / 增加升级脚本 / 修正 SQL 示例 / 对齐真实环境 schema”】

数据库信息：
- 涉及文件：【填写，例如 `sql/init.sql`、`sql/init-data.sql`、`sql/upgrade/*.sql`】
- 涉及表/视图：【填写】
- 是新库初始化还是历史库升级：【填写】
- 是否要求幂等：【填写】
- 是否允许破坏性变更：【填写】
- 是否有真实环境兼容要求：【填写】

要求：
- 先按模板要求阅读最小阅读集
- 先判断是初始化问题还是升级兼容问题
- 直接修改 SQL / 脚本 / 文档，不要只给方案
- 完成后输出变更文件、结构变化说明、执行方式、兼容性风险和同步更新的文档