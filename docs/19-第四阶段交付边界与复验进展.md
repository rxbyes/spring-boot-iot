# 19 第四阶段交付边界与复验进展（权威）

> 文档定位：第四阶段交付边界、阻塞项、真实环境复验和阶段收口状态的唯一权威文档。
> 适用角色：项目负责人、研发、测试、运维、交付负责人。
> 权威级别：阶段唯一权威。
> 上游来源：当前代码、真实环境复验事实、阶段任务推进记录。
> 下游消费：README、验收矩阵、阶段规划、交付边界说明。
> 变更触发条件：第四阶段已交付范围、阻塞项、复验结论、验收前置变化。
> 更新时间：2026-04-02

权威声明：本文件为第四阶段进度、后续开发与验收安排的唯一权威来源。如与 README.md、AGENTS.md 或其他路线图冲突，以本文件为准。
职责边界：
- 本文件是第四阶段交付边界、阻塞项、真实环境复验和收口状态的唯一权威。
- 通用阶段路线、下一阶段入口和里程碑建议，请参考 [16-阶段规划与迭代路线图.md](./16-阶段规划与迭代路线图.md)。
- 交付能力矩阵与验收判定，请参考 [21-业务功能清单与验收标准.md](./21-业务功能清单与验收标准.md)。

## 1. 基本约束

- 项目名称：`spring-boot-iot`
- 启动模块：`spring-boot-iot-admin`
- Base package：`com.ghlzm.iot`
- 必须保持 Phase 1-3 主链路不回归：HTTP/MQTT 上报、指令闭环、网关/子设备拓扑、规则引擎。
- 不破坏模块边界，不把持久化逻辑放进协议适配层，不把业务逻辑放进 Controller。
- 不引入不必要的重依赖。
- 消息日志表命名统一以 `iot_message_log` 为主，历史兼容关系由迁移方案处理。
- Phase 4 验收统一走真实环境，不再使用旧 H2 验收剖面。
- 真实环境默认参考 `spring-boot-iot-admin/src/main/resources/application-dev.yml`，可通过环境变量覆盖：
  - `IOT_MYSQL_URL`、`IOT_MYSQL_USERNAME`、`IOT_MYSQL_PASSWORD`
  - `IOT_TDENGINE_URL`、`IOT_TDENGINE_USERNAME`、`IOT_TDENGINE_PASSWORD`
  - `IOT_REDIS_HOST`、`IOT_REDIS_PORT`、`IOT_REDIS_PASSWORD`、`IOT_REDIS_DATABASE`
  - `IOT_MQTT_ENABLED`、`IOT_MQTT_BROKER_URL`、`IOT_MQTT_CLIENT_ID`、`IOT_MQTT_USERNAME`、`IOT_MQTT_PASSWORD`
  - `spring.cloud.aes.merchants.{appId}`

## 2. 当前进度（以本文件为准）

### 2.1 已完成并已具备交付基础
- 告警运营台：后端已完成，前端页面已完成。
- 事件协同台：后端已完成，前端页面已完成。
- 风险对象中心：后端已完成，前端页面已完成。
- 阈值策略：后端已完成，前端页面已完成。
- 联动编排与应急预案库：后端已完成，前端页面已完成。
- 运营分析中心：后端真实统计已完成，前端页面已完成。
- 风险监测实时监测 / GIS 态势图：2026-03-24 已在当前共享开发环境完成后端接口、前端页面与详情抽屉的真实环境复验，正式纳入 Phase 4 交付基线。
- 平台治理：组织架构、账号中心、角色权限、导航编排、区域版图、数据字典、通知编排、审计中心已完成基础交付；2026-03-16 已补齐基于 MySQL 的动态菜单、角色菜单关系、用户角色关系与按钮权限基线，2026-03-17 已补齐导航编排页与菜单 CRUD 接口基线。
  - 2026-03-17 已补齐平台治理与风险运营列表页统一表格操作栏基线；当前正式口径已按 `StandardPageShell + StandardWorkbenchPanel` 主工作台收口，列表页是否展示“已选计数 / 清空选中 / 刷新列表”取决于页面是否存在多选与批量操作，实时监测台与 GIS 态势图默认只保留“筛选 + 刷新 + 正文分区”合同。
  - 2026-03-17 已补齐平台治理与风险运营列表页“导出选中”CSV 能力，采用前端本地导出，不增加后端接口负担。
  - 2026-03-17 已补齐 CSV 导出的中文列头与关键状态文案映射，导出内容可直接用于业务复核。
  - 2026-03-17 已补齐“导出当前结果”CSV 能力，默认导出当前页面列表已加载数据，支持与筛选动作联动。
  - 2026-03-17 已补齐“导出列设置”能力（列勾选 + 顺序调整 + 本地持久化），增强业务导出灵活性。
  - 2026-03-17 已补齐“导出模板”能力（默认/运维/管理），支持按角色侧重点快速切换导出字段集。
  - 2026-03-17 已补齐“自定义导出模板”能力（保存/删除），并按页面维度本地持久化。
  - 2026-03-17 已补齐“模板JSON导入/导出”能力，支持导出模板共享与导入复用。
  - 2026-03-17 已补齐模板重命名与分组标签能力，支持按分组筛选并快速定位模板。
  - 2026-03-17 已补齐模板搜索与最近使用置顶能力，并展示模板最近使用时间、最后修改时间。
  - 2026-03-17 已补齐模板导入预览明细能力，支持查看新增/覆盖/重命名/跳过完整列表，并可在明细弹窗直接确认导入。
  - 2026-03-17 已补齐平台治理分页工程化收口：账号、角色、数据字典、通知编排改为真实后端分页；组织架构、区域版图、导航编排切换为“根节点分页 + 子节点懒加载”；审计中心与异常观测台统一改为数据库分页。
  - 2026-03-17 已补齐前后端分页抽象：后端新增 `PageQueryUtils` 统一分页参数归一化与 `PageResult` 转换，前端新增 `useServerPagination` 统一页码、页长、总数和翻页重置逻辑。
  - 2026-03-17 已补齐真实环境分页索引：新增 `20260317_phase4_system_governance_paging_indexes.sql`，并同步写入 `sql/init.sql`，用于降低平台治理分页、树表懒加载与异常观测查询的全表扫描风险。
  - 2026-03-18 已统一平台治理列表的长文本展示：组织架构、账号中心、角色权限、区域版图、数据字典、通知编排、导航编排及字典项子表默认单行省略，鼠标悬停显示完整内容，去除列表内自动换行，展示口径向审计中心看齐。
  - 2026-03-16 已补齐后端自动审计采集链路：`/api/**` 请求会自动写入 `sys_audit_log`（排除 `/api/system/audit-log/**` 与 `/api/auth/login`）。
  - 2026-03-17 已补齐审计失败识别与后台异常落库：统一响应体 `code != 200` 的 HTTP 请求会标记为失败；MQTT 启动失败、订阅失败、连接断开、消息分发失败等异步异常会以 `operation_type=system_error` 写入 `sys_audit_log`。
  - 2026-03-17 已补齐系统异常自动通知链路：支持通过 `iot.observability.system-error-notify-enabled` 开启 `system_error` 场景通知，通知渠道页支持 `webhook` 类型配置与 `POST /api/system/channel/test/{channelCode}` 测试发送。
  - 2026-03-17 已补齐统一链路 `traceId`：HTTP / MQTT / 消息日志 / 系统异常审计共享同一追踪号，并在日志中落库 `trace_id`、设备编码、产品标识等字段。
  - 2026-03-17 已轻量化顶部工具区：搜索框收口为短占位轻搜索框，通知/帮助改为短标签入口，账号区默认只展示头像与账号名，详细角色和认证信息下钻到悬浮菜单。
  - 2026-03-17 已补齐控制台导航减噪：左侧分组简介卡仅在分组首页展示；右上角头像菜单统一承载账号中心、实名认证、登录方式管理、修改密码、退出登录。
  - 2026-03-17 已补齐一级分组首页：新增 `/device-access`、`/risk-disposal`、`/risk-config`、`/system-management`、`/quality-workbench` 作为一级导航的统一落地页，先展示分组概览，再进入具体页面。
  - 2026-03-17 已细化一级分组首页：首屏改为 `常用入口 / 最近使用 / 推荐操作 / 全部能力` 结构，并结合本地 activity 痕迹回推最近入口，减少重复导航说明。
  - 2026-03-17 已精简一级顶栏：一级导航仅保留主标题，分组说明改为悬浮提示；分组首页标题区不再重复显示说明文案。
  - 2026-03-17 已轻量化二级导航：左侧菜单仅保留名称，说明改为悬浮提示；展开态短标识降为弱提示圆点，折叠态再保留缩写识别，激活项通过高亮底色与侧边指示线强化当前定位。
  - 2026-03-17 已压缩页面标题区：内容区页头改为“分组眉题 + 主标题 + 必要说明”的轻层级，移除厚卡片式外壳并收紧上下留白。
  - 2026-03-17 已统一首页与分组首页卡片风格：面板、指标卡、工作台入口和推荐卡片统一收敛圆角、阴影与按钮力度，整体视觉更接近云控制台。
  - 2026-03-18 已继续统一公共壳层：顶部导航重组为“品牌区 / 搜索区 / 工具区 + 业务分区条”的双层结构，一级导航左对齐并统一激活态；左侧导航统一补齐分组摘要、菜单数量与页面类型提示；内容区页头升级为“面包屑 + 标题说明 + 状态卡 + 返回概览入口”的标准控制台页头。
  - 2026-03-18 已统一业务工作台细节：告警运营台、事件协同台、实时监测台、GIS态势图的提示条、列表操作条、空态卡与 GIS 资源卡进一步收口为浅色轻卡片风格，列表区观感更接近云控制台。
  - 2026-03-18 已继续统一详情抽屉：`StandardDetailDrawer` 外壳进一步收敛标题标签、概览卡、字段卡、提示卡与最近事件/告警卡的圆角、阴影和胶囊标签样式，业务详情页与日志详情页视觉节奏保持一致。
  - 接入智维排障：2026-03-17 已将 `system_error` 后台异常从审计中心入口拆分为 `/system-log` 独立异常观测台，面向研发、测试、运维排查接入链路与后台异常。
- 接入智维追踪：2026-03-17 已新增 `/message-trace` 页面，形成“链路追踪台”，支持按 `TraceId`、设备编码、产品标识、消息类型和 Topic 分页检索 `iot_message_log`，并与 `/system-log` 双向联动；当前兼容视图底层仍来源于物理表 `iot_device_message_log`。
- 日志能力收敛优化：2026-03-18 已补齐业务审计统计接口 `GET /api/system/audit-log/business/stats`，并在业务日志/系统日志页统一支持 `operationResult`（成功/失败）筛选与统计概览，建设目标聚焦“业务可审计、研发可排障”。
- 角色化工作台：2026-03-19 已完成五工作台一级导航与角色默认首页落地，当前默认结构为 `接入智维 / 风险运营 / 风险策略 / 平台治理 / 质量工场`；业务/管理角色默认进入 `/risk-disposal`，运维/研发角色默认进入 `/device-access`，超级管理员默认进入 `/system-management`。
- 深部位移自动闭环：2026-03-21 已补齐 `$dp` 深部位移子设备链路的服务端自动闭环，属性落库后可按 `risk_point_device` 绑定自动完成红橙黄蓝判级、黄橙红告警、橙/红事件与自动派工，并把联动规则/应急预案命中结果写入告警/事件留痕字段。
- 2026-04-02 运行期统一补强：后端已把 `rule_definition` 接入自动闭环判级，未命中策略时才回退 `iot.alarm.auto-closure.*` 默认阈值；`risk-monitoring` 读侧风险等级已改为优先消费运行态告警/事件结果，并新增 `/api/risk-governance/missing-bindings`、`/api/risk-governance/missing-policies` 缺口接口。当天已在 `application-dev` 基线下通过临时端口 `10099` 启动新包完成真实环境核验，登录、`missing-bindings`、`missing-policies` 均返回 `code=200`；共享 `9999` 端口因旧实例占用未直接替换。
- 2026-04-02 风险治理实钻补记：使用当前代码实例 `10099`、专用演练设备 `CDXDD10099A1`、专用风险点 `codex-drill-risk-point-a2` 补齐了 `设备上报 -> 风险对象绑定 -> 阈值策略 -> 联动规则 -> 应急预案 -> 实时监测 -> 告警 -> 事件 -> 工单` 全链路演练。过程中补修了两个真实缺口：
  - `RiskPointServiceImpl` 现已兼容共享 dev 环境仍保留 `critical / warning / info` 旧字典值的 `risk_level` 配置，风险点新增/编辑不再因字典未升级而直接失败。
  - `RiskPolicyResolver` 现已在同一测点命中多条规则时按最高严重度选取命中策略，不再依赖数据库 `create_time` 返回顺序，避免 `>= 20` 红色规则被同秒写入的橙色规则抢先命中。
  - 实钻结果：蓝色 `0.0014` 仅更新监测态为 `NORMAL/blue`；黄色 `6.2` 生成 `1` 条 `medium` 告警；橙色 `12.8` 生成累计 `2` 条告警、`1` 条 `orange` 事件和 `1` 张工单；红色 `21.6` 生成累计 `3` 条告警、`2` 条事件和 `2` 张工单，最新告警/事件留痕已同时写入联动规则与应急预案命中结果。
- 2026-04-02 风险策略兼容补记：`/api/rule-definition/list`、`/api/rule-definition/page` 已补齐 `ruleName` 模糊筛选，并把告警等级查询兼容到 `critical / warning / info`、`red / orange / yellow / blue`、`high / medium / low` 混合库存；`/api/emergency-plan/list`、`/api/emergency-plan/page` 在传入 `red / orange / blue` 时会同时命中历史 `critical / warning / info` 预案，阈值策略页与应急预案页的统计/标签/编辑回显也同步归一到当前前端口径。

### 2.2 2026-03-24 风险监测真实环境复验结论
- 已按 `spring-boot-iot-admin/src/main/resources/application-dev.yml` 启动后端与前端，并完成 `admin / 123456` 登录、接口联调和页面巡检。
- 后端接口复验通过：
  - `GET /api/risk-monitoring/realtime/list?pageNum=1&pageSize=10` 返回 `200`，当前共享开发环境可查询到 `16` 条实时监测记录。
  - `GET /api/risk-monitoring/gis/points` 返回 `200`，当前共享开发环境可查询到 `28` 个 GIS 点位对象。
  - `GET /api/risk-monitoring/realtime/8110` 返回 `200`，详情接口可查询 `24h` 趋势、最近告警与最近事件。
- 前端页面复验通过：
  - `/risk-monitoring` 可加载真实记录、筛选条件与详情抽屉。
  - `/risk-monitoring-gis` 可展示点位态势、未定位风险点列表，并复用统一详情抽屉。
- 风险监测正式纳入口径：
  - `RealTimeMonitoringView`
  - `RiskGisView`
  - `RiskMonitoringDetailDrawer`
  - `/risk-monitoring`
  - `/risk-monitoring-gis`

### 2.2.1 历史前置与旧环境说明
- 仓库 SQL 入口当前只保留 `sql/init.sql` 与 `sql/init-data.sql`。
- 当前共享开发环境已具备风险监测所需 schema，不再把历史增量脚本视为本环境默认阻塞项。
- 若其他环境仍存在 Phase 4 早期结构偏差（缺表、缺列、旧约束），应先按最新初始化基线重建或人工比对，再复验。

### 2.3 明确未交付项
- GIS SDK / 第三方地图底图能力未交付。
- GIS 热力图、地图底图交互、驾驶舱内嵌风险监测子模块未交付。
- 首页驾驶舱已完成商业化重构，并升级为“数据看板驾驶舱 + 事务工作台入口”双层结构，支持一线/运维/管理/研发四类角色视角；当前仍采用静态能力编排与本地 activity 兜底，不纳入真实环境验收。
- 设备中心下一阶段只冻结“产品物模型设计器”一条增强口子；截至 2026-03-25，已完成真实环境接口、数据库与页面复验，但该能力仍不计入 Phase 4 已交付范围。
- 上下架审批、远程维护、维修工单联动继续保留为候补，不与产品物模型设计器并行启动。

## 3. 当前轮次必须持续推进的工作项

### 3.1 P0 质量与验收
- 维护真实环境验收口径，不再新增端到端验收入口。
- 持续补齐 `docs/真实环境测试与验收手册.md`、`docs/14-MQTTX真实环境联调手册.md` 等真实环境验收文档。
- 持续维护 Phase 4 真实环境验收证据，保留接口、页面、数据库、Redis、MQTTX 证据。
- 2026-03-24 已在当前共享开发环境执行 `python3 scripts/run-message-flow-acceptance.py`，HTTP / MQTT 主链路、十阶段时间线、`/api/device/message-flow/ops/overview` 与 `recent` 查询均复验通过；本轮未提供 `--expired-trace-id`，expired trace 降级链路继续按需单独取证。

### 3.2 P0 数据一致性
- 2026-03-24 已完成 `iot_message_log` 主命名口径收口。
- 所有新文档、新接口说明、新验收口径一律优先使用 `iot_message_log`。
- 历史实现仍使用旧物理表 `iot_device_message_log` 时，必须显式标注“`iot_message_log` 为兼容视图”的关系，避免新开发继续扩散旧命名。

### 3.3 P1 风险监测基线固化
- 持续保持实时监测、GIS 态势图与详情抽屉在真实环境可复验。
- 历史库或旧环境首次联调风险监测时，先执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql`，再补做接口与页面复验。
- GIS SDK、热力图、地图底图交互与驾驶舱内嵌子模块继续留在未交付范围，不随本次复验一并宣称交付。

### 3.4 P1 文档回写
- 只要接口、页面结构、验收方式、环境变量发生变化，必须同步回写 `docs/`。
- `README.md` 与 `AGENTS.md` 如无新增行为差异，可保持现状；如新增启动口径或验收脚本，再同步更新。

### 3.5 P1 设备中心下一子项冻结
- 2026-03-24 已确认设备中心下一阶段只开“产品物模型设计器”一条增强口子，独立任务卡见 [2026-03-24-product-model-designer-implementation-plan.md](./superpowers/plans/2026-03-24-product-model-designer-implementation-plan.md)。
- 选型依据：`ProductWorkbenchView` 已具备产品台账、接入契约和关联设备闭环；`DeviceWorkbenchView` 已具备设备资产、父子拓扑、批量导入、设备更换与详情闭环。
- 2026-03-24 当前分支已补齐 `ProductModelController` 与产品维度 CRUD：`GET/POST/PUT/DELETE /api/device/product/{productId}/models...`，并在 `ProductWorkbenchView` 内挂载独立设计器入口，不新增顶层路由。
- 2026-03-25 当前共享开发环境已完成该能力的接口、数据库与页面复验；当前仓库未保留独立 JSON 证据文件，留痕以本页记录、任务卡与《真实环境测试与验收手册》补充章节为准。
- 当前剩余重点已转为字段治理细化、统一 CI / 契约测试门禁和交付范围管控；`ProductController` 继续只承接产品台账 CRUD，`ProductModelController` 独立承接物模型治理。
- 设备资产扩展、上下架审批、远程维护和维修工单联动继续保留为候补，不与该任务卡并行。

## 4. Phase 4 收口里程碑建议

- M1：风险监测真实环境联调完成（已于 2026-03-24 完成）。
- M2：Phase 4 核心模块真实环境验收闭环完成（已于 2026-03-24 完成）。
- M3：`iot_message_log` 主命名口径收口完成（已于 2026-03-24 完成）。
- M4：首页商业化重构完成并稳定维护，设备中心下一条增强已冻结为产品物模型设计器，并已完成真实环境接口、数据库与页面复验。

## 5. 持续跟进任务清单

| 优先级 | 模块 | 任务 | 当前状态 | 产出 | 验收点 |
|---|---|---|---|---|---|
| P0 | 质量保障 | Phase 4 真实环境验收清单与脚本整理 | 进行中 | `docs/真实环境测试与验收手册.md`、验收脚本、核对步骤 | 关键场景可按文档执行 |
| P0 | 数据一致性 | `iot_message_log` 命名统一方案 | 已于 2026-03-24 完成主口径收口 | 活跃文档、接口说明与验收口径统一使用 `iot_message_log`，并显式说明兼容视图关系 | 不影响已验证主链路 |
| P1 | 风险监测 | 实时监测 API 复验 | 已于 2026-03-24 完成真实环境复验 | `/api/risk-monitoring/realtime/list` | 当前共享开发环境返回 `200` 且可查询真实监测记录；旧环境首次联调前按需执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql` |
| P1 | 风险监测 | 详情抽屉 API 复验 | 已于 2026-03-24 完成真实环境复验 | `/api/risk-monitoring/realtime/{bindingId}` | 24h 趋势、最近告警、最近事件可查询 |
| P1 | 风险监测 | 实时监测页联调 | 已于 2026-03-24 完成真实环境复验 | `RealTimeMonitoringView` | 列表、筛选、详情抽屉可用 |
| P1 | GIS | GIS 点位 API 复验 | 已于 2026-03-24 完成真实环境复验 | `/api/risk-monitoring/gis/points` | 当前共享开发环境返回点位与未定位对象数据；旧环境首次联调前按需执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql` |
| P1 | GIS | GIS 页面联调 | 已于 2026-03-24 完成真实环境复验 | `RiskGisView` | 点位渲染、未定位列表、详情抽屉可用 |
| P2 | 驾驶舱 | 首页信息架构商业化重构 | 已完成 | `CockpitView` 首页改版 | 去除无关入口，并围绕业务闭环组织首页 |
| P2 | 驾驶舱 | 首页角色视角与待办引导 | 已完成 | 角色 preset、经营入口卡片、待办中心与 activity 痕迹 | 首屏不依赖 `/api/cockpit/*` 真实聚合接口 |
| P2 | 设备中心 | 产品物模型设计器 | 已于 2026-03-25 完成真实环境接口、数据库与页面复验，继续作为下一阶段唯一子项推进 | 独立任务卡、物模型管理入口、后端 CRUD、`/products` 内设计器抽屉、验收证据文件 | 不与上下架审批、远程维护、维修工单联动并行；后续聚焦字段治理与 CI / 契约测试门禁 |

补充说明：
- 设备资产扩展、上下架审批、远程维护、维修工单联动当前不单独立项，继续保留为后续候补。

## 6. 风险监测模块实现口径

### 6.1 页面拆分
- 实时监测与 GIS 风险态势必须保持两条独立路由，不做单页双 Tab。
- 详情统一使用右侧抽屉，不新增独立详情路由。
- `Cockpit` 只保留跳转入口，不承接本轮风险监测实现。

### 6.2 实时监测页要求
- 路由：`/risk-monitoring`
- 筛选项：区域、风险点、设备编码、风险等级、在线状态
- 列表字段：设备编码、设备名称、产品名称、风险点、测点、当前值、状态、最新上报时间、风险等级、告警标记
- 交互：点击“查看详情”打开统一详情抽屉

### 6.3 GIS 页要求
- 路由：`/risk-monitoring-gis`
- 可视化：基于 ECharts 的经纬度散点态势图，不引入第三方地图 SDK
- 编码规则：颜色映射风险等级，点大小映射活跃告警数
- 兜底要求：无经纬度数据的风险点必须进入“未定位风险点”列表
- 当前轮次不承诺底图、瓦片服务、热力图或 GIS SDK 能力

### 6.4 详情抽屉要求
- 展示当前监测信息
- 展示最近 24h 趋势
- 展示最近告警
- 展示最近事件
- 支持从列表页与 GIS 页复用

## 7. 验收与回归

- 构建：`mvn -s .mvn/settings.xml clean package -DskipTests`；若仓库不存在 `.mvn/settings.xml`，直接改为 `mvn clean package -DskipTests`
- 后端运行：`mvn -s .mvn/settings.xml -pl spring-boot-iot-admin spring-boot:run -Dspring-boot.run.profiles=dev`；若仓库不存在 `.mvn/settings.xml`，直接省略 `-s .mvn/settings.xml`
- 前端运行：`cd spring-boot-iot-ui && npm install && npm run acceptance:dev`
- 真实环境验收参考：`docs/真实环境测试与验收手册.md`、`docs/14-MQTTX真实环境联调手册.md`、`docs/03-接口规范与接口清单.md`

## 8. 更新记录

| 日期 | 更新内容 | 影响模块 | 责任人 | 备注 |
|---|---|---|---|---|
| 2026-03-16 | 确认本文件为 Phase 4 唯一权威进度文档 | 全局 | - | 后续冲突以本文件为准 |
| 2026-03-16 | 风险监测前端状态修正为“已完成开发待真实环境验证” | UI / alarm | Codex | GIS SDK 与底图能力仍未交付 |
| 2026-03-16 | 验收口径统一切换到真实环境，停止使用旧端到端验收链路 | 全局 | Codex | 参考 `application-dev.yml` 环境变量 |
| 2026-03-16 | 报表分析接口改为真实业务表聚合，并固定前端日期参数为 YYYY-MM-DD | report / UI | Codex | ReportServiceImpl 不再是连通性占位实现 |
| 2026-03-16 | 新增风险监测 schema 对齐脚本并补充共享开发库阻塞说明，明确需先执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql` 后再复验 | alarm / SQL / docs | Codex | 阻塞项集中在 `/api/risk-monitoring/*` |
| 2026-03-16 | 首页驾驶舱完成商业化重构，并补齐角色视角 preset 与待办中心，当前仍不纳入真实环境验收 | UI | Codex | 首页主视图采用静态能力编排与 activity 兜底 |
| 2026-03-17 | 平台治理分页基线补齐：账号、角色、数据字典、通知编排改为真实后端分页；组织架构、区域版图、导航编排改为“根节点分页 + 子节点懒加载”；审计中心分页改为数据库分页 | system / UI / docs | Codex | 解决平台治理页前端假分页与后端全量查询问题 |
| 2026-03-17 | 平台治理分页工程化补强：新增 `PageQueryUtils`、`useServerPagination` 与历史库分页索引脚本，统一前后端分页收口并降低真实库分页查询成本 | framework / system / UI / SQL / docs | Codex | 覆盖分页参数归一化、分页状态复用与历史库索引补齐 |
| 2026-03-18 | 日志能力收敛优化：新增业务审计统计接口，业务日志/系统日志统一补齐 `operationResult` 筛选和统计概览，明确“业务审计 + 后端排障”双目标 | system / UI / docs | Codex | 避免建设目标发散，优先增强现有日志链路可用性 |
| 2026-03-18 | 公共壳层继续商业化收口：顶部导航、左侧菜单和内容页头统一按阿里云式浅色控制台重组，提升导航层级和页面定位的一致性 | UI / docs | Codex | 不改业务路由与权限，仅收敛展示规范与信息架构 |
| 2026-03-19 | 角色化菜单工作台落地：一级导航统一为 `接入智维 / 风险运营 / 风险策略 / 平台治理 / 质量工场`，并同步补齐角色默认首页、菜单种子与增量升级脚本 | system / UI / SQL / docs | Codex | 旧三大分区口径退出前端主导航 |
| 2026-03-21 | 深部位移自动闭环落地：`$dp` 子设备入库后新增平台内自动判级、自动告警、自动事件/派工与联动/预案命中留痕 | device / alarm / framework / docs | Codex | 继续复用现有 API，不新增表 |
| 2026-03-24 | 风险监测实时监测 / GIS 态势图完成当前共享开发环境真实环境复验，并正式纳入 Phase 4 交付基线 | alarm / UI / docs | Codex | GIS SDK、底图、热力图与驾驶舱内嵌能力仍不纳入交付 |
| 2026-04-02 | 风险策略运行期统一：`rule_definition` 接入自动闭环，`risk-monitoring` 改为优先展示运行态风险等级，并新增风险治理缺口 API | alarm / device / docs | Codex | 已完成后端模块定向单测，待共享 `dev` 环境复验 |
| 2026-03-24 | 设备中心下一阶段只冻结“产品物模型设计器”一条增强线，并新增独立任务卡；上下架审批、远程维护、维修工单联动暂不并行 | device / UI / docs | Codex | 防止设备中心后续范围继续扩散 |
| 2026-03-24 | 当前共享开发环境完成 `message-flow` 真实环境复验：HTTP / MQTT 主链路、十阶段时间线、`opsOverview` 与 `recent` 查询均通过 | message / device / docs | Codex | 本轮未提供 `--expired-trace-id`，过期 trace 降级链路仍保留单独取证 |
| 2026-03-24 | 产品物模型设计器完成首轮本地实现：补齐产品维度物模型 CRUD、`/products` 工作台入口、独立设计器抽屉及定向测试 / 前端构建验证 | device / UI / docs | Codex | 继续作为下一阶段增强推进，不计入 Phase 4 已交付范围 |
| 2026-03-25 | 产品物模型设计器完成当前共享开发环境真实环境复验：`property / event / service` CRUD、旧 schema 兼容、数据库留痕与 `/products` 页面抽屉均通过 | device / UI / docs | Codex | 当前仓库未归档独立 JSON 证据文件，留痕以本页记录、任务卡与《真实环境测试与验收手册》补充章节为准；仍不并入 Phase 4 已交付范围 |

## 日志收敛补充（2026-03-18）
- 已补齐“系统异常到消息追踪”的最小链路兜底：当 MQTT 消息在分发前置校验阶段失败时，补写 `iot_message_log` 追踪记录（messageType=`dispatch_failed`，当前物理写入仍落 `iot_device_message_log`）。
- 目标保持不变：业务日志服务审计，系统日志服务后端排障；本次仅增强链路可追溯性，不扩散到其他建设范围。
