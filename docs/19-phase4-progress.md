# Phase 4 持续跟进与开发指导（权威）

更新时间：2026-03-18
权威声明：本文件为 Phase 4 进度、后续开发与验收安排的唯一权威来源。如与 README.md、AGENTS.md 或其他路线图冲突，以本文件为准。

## 1. 基本约束

- 项目名称：`spring-boot-iot`
- 启动模块：`spring-boot-iot-admin`
- Base package：`com.ghlzm.iot`
- 必须保持 Phase 1-3 主链路不回归：HTTP/MQTT 上报、指令闭环、网关/子设备拓扑、规则引擎。
- 不破坏模块边界，不把持久化逻辑放进协议适配层，不把业务逻辑放进 Controller。
- 不引入不必要的重依赖。
- 消息日志表命名统一以 `iot_message_log` 为主，历史兼容关系由迁移方案处理。
- Phase 4 验收统一走真实环境，不再使用旧 H2 验收剖面。
- 真实环境默认参考 `spring-boot-iot-admin/src/main/resources/application-dev.yml`，可通过环境变量覆盖：
  - `IOT_MYSQL_URL`、`IOT_MYSQL_USERNAME`、`IOT_MYSQL_PASSWORD`
  - `IOT_TDENGINE_URL`、`IOT_TDENGINE_USERNAME`、`IOT_TDENGINE_PASSWORD`
  - `IOT_REDIS_HOST`、`IOT_REDIS_PORT`、`IOT_REDIS_PASSWORD`、`IOT_REDIS_DATABASE`
  - `IOT_MQTT_ENABLED`、`IOT_MQTT_BROKER_URL`、`IOT_MQTT_CLIENT_ID`、`IOT_MQTT_USERNAME`、`IOT_MQTT_PASSWORD`
  - `spring.cloud.aes.merchants.{appId}`

## 2. 当前进度（以本文件为准）

### 2.1 已完成并已具备交付基础
- 告警中心：后端已完成，前端页面已完成。
- 事件处置：后端已完成，前端页面已完成。
- 风险点管理：后端已完成，前端页面已完成。
- 阈值规则配置：后端已完成，前端页面已完成。
- 联动规则与应急预案：后端已完成，前端页面已完成。
- 分析报表：后端真实统计已完成，前端页面已完成。
- 系统管理：组织、用户、角色、菜单、区域、字典、通知渠道、业务日志已完成基础交付；2026-03-16 已补齐基于 MySQL 的动态菜单、角色菜单关系、用户角色关系与按钮权限基线，2026-03-17 已补齐菜单管理页与菜单 CRUD 接口基线。
  - 2026-03-17 已补齐系统管理与风险处置列表页统一表格操作栏（已选计数、清空选中、刷新列表），公共列表交互基线完成落地。
  - 2026-03-17 已补齐系统管理与风险处置列表页“导出选中”CSV 能力，采用前端本地导出，不增加后端接口负担。
  - 2026-03-17 已补齐 CSV 导出的中文列头与关键状态文案映射，导出内容可直接用于业务复核。
  - 2026-03-17 已补齐“导出当前结果”CSV 能力，默认导出当前页面列表已加载数据，支持与筛选动作联动。
  - 2026-03-17 已补齐“导出列设置”能力（列勾选 + 顺序调整 + 本地持久化），增强业务导出灵活性。
  - 2026-03-17 已补齐“导出模板”能力（默认/运维/管理），支持按角色侧重点快速切换导出字段集。
  - 2026-03-17 已补齐“自定义导出模板”能力（保存/删除），并按页面维度本地持久化。
  - 2026-03-17 已补齐“模板JSON导入/导出”能力，支持导出模板共享与导入复用。
  - 2026-03-17 已补齐模板重命名与分组标签能力，支持按分组筛选并快速定位模板。
  - 2026-03-17 已补齐模板搜索与最近使用置顶能力，并展示模板最近使用时间、最后修改时间。
  - 2026-03-17 已补齐模板导入预览明细能力，支持查看新增/覆盖/重命名/跳过完整列表，并可在明细弹窗直接确认导入。
  - 2026-03-17 已补齐系统治理分页工程化收口：用户、角色、字典、通知渠道改为真实后端分页；组织、区域、菜单切换为“根节点分页 + 子节点懒加载”；业务日志与系统日志统一改为数据库分页。
  - 2026-03-17 已补齐前后端分页抽象：后端新增 `PageQueryUtils` 统一分页参数归一化与 `PageResult` 转换，前端新增 `useServerPagination` 统一页码、页长、总数和翻页重置逻辑。
  - 2026-03-17 已补齐真实环境分页索引：新增 `20260317_phase4_system_governance_paging_indexes.sql`，并同步写入 `sql/init.sql`，用于降低系统治理分页、树表懒加载与系统日志查询的全表扫描风险。
  - 2026-03-18 已统一系统治理列表的长文本展示：组织、用户、角色、区域、字典、通知渠道、菜单及字典项子表默认单行省略，鼠标悬停显示完整内容，去除列表内自动换行，展示口径向业务日志页看齐。
  - 2026-03-16 已补齐后端自动审计采集链路：`/api/**` 请求会自动写入 `sys_audit_log`（排除 `/api/system/audit-log/**` 与 `/api/auth/login`）。
  - 2026-03-17 已补齐审计失败识别与后台异常落库：统一响应体 `code != 200` 的 HTTP 请求会标记为失败；MQTT 启动失败、订阅失败、连接断开、消息分发失败等异步异常会以 `operation_type=system_error` 写入 `sys_audit_log`。
  - 2026-03-17 已补齐系统异常自动通知链路：支持通过 `iot.observability.system-error-notify-enabled` 开启 `system_error` 场景通知，通知渠道页支持 `webhook` 类型配置与 `POST /api/system/channel/test/{channelCode}` 测试发送。
  - 2026-03-17 已补齐统一链路 `traceId`：HTTP / MQTT / 消息日志 / 系统异常审计共享同一追踪号，并在日志中落库 `trace_id`、设备编码、产品标识等字段。
  - 2026-03-17 已轻量化顶部工具区：搜索框收口为短占位轻搜索框，通知/帮助改为短标签入口，账号区默认只展示头像与账号名，详细角色和认证信息下钻到悬浮菜单。
  - 2026-03-17 已补齐控制台导航减噪：左侧分组简介卡仅在分组首页展示；右上角头像菜单统一承载账号中心、实名认证、登录方式管理、修改密码、退出登录。
  - 2026-03-17 已补齐一级分组首页：新增 `/device-access`、`/risk-disposal`、`/system-management`（以及风险增强概览页）作为一级导航的统一落地页，先展示分组概览，再进入具体页面。
  - 2026-03-17 已细化一级分组首页：首屏改为 `常用入口 / 最近使用 / 推荐操作 / 全部能力` 结构，并结合本地 activity 痕迹回推最近入口，减少重复导航说明。
  - 2026-03-17 已精简一级顶栏：一级导航仅保留主标题，分组说明改为悬浮提示；分组首页标题区不再重复显示说明文案。
  - 2026-03-17 已轻量化二级导航：左侧菜单仅保留名称，说明改为悬浮提示；展开态短标识降为弱提示圆点，折叠态再保留缩写识别，激活项通过高亮底色与侧边指示线强化当前定位。
  - 2026-03-17 已压缩页面标题区：内容区页头改为“分组眉题 + 主标题 + 必要说明”的轻层级，移除厚卡片式外壳并收紧上下留白。
  - 2026-03-17 已统一首页与分组首页卡片风格：面板、指标卡、工作台入口和推荐卡片统一收敛圆角、阴影与按钮力度，整体视觉更接近云控制台。
  - 2026-03-18 已继续统一公共壳层：顶部导航重组为“品牌区 / 搜索区 / 工具区 + 业务分区条”的双层结构，一级导航左对齐并统一激活态；左侧导航统一补齐分组摘要、菜单数量与页面类型提示；内容区页头升级为“面包屑 + 标题说明 + 状态卡 + 返回概览入口”的标准控制台页头。
  - 2026-03-18 已统一业务工作台细节：告警中心、事件处置、实时监测、GIS 风险态势的提示条、列表操作条、空态卡与 GIS 资源卡进一步收口为浅色轻卡片风格，列表区观感更接近云控制台。
  - 2026-03-18 已继续统一详情抽屉：`StandardDetailDrawer` 外壳进一步收敛标题标签、概览卡、字段卡、提示卡与最近事件/告警卡的圆角、阴影和胶囊标签样式，业务详情页与日志详情页视觉节奏保持一致。
  - 设备接入调试：2026-03-17 已将 `system_error` 后台异常从业务日志入口拆分为 `/system-log` 独立系统日志页，面向研发、测试、运维排查设备接入与后台链路问题。
- 设备接入追踪：2026-03-17 已新增 `/message-trace` 页面，支持按 `TraceId`、设备编码、产品标识、消息类型和 Topic 分页检索 `iot_device_message_log`，并与 `/system-log` 双向联动。
- 日志能力收敛优化：2026-03-18 已补齐业务审计统计接口 `GET /api/system/audit-log/business/stats`，并在业务日志/系统日志页统一支持 `operationResult`（成功/失败）筛选与统计概览，建设目标聚焦“业务可审计、研发可排障”。

### 2.2 已完成开发，待真实环境联调验证
- 风险监测后端只读接口：
  - `GET /api/risk-monitoring/realtime/list`
  - `GET /api/risk-monitoring/realtime/{bindingId}`
  - `GET /api/risk-monitoring/gis/points`
- 风险监测前端页面：
  - `RealTimeMonitoringView` 已完成开发，待真实环境联调验证。
  - `RiskGisView` 已完成开发，待真实环境联调验证。
  - `RiskMonitoringDetailDrawer` 已完成开发，待真实环境联调。
- 风险监测路由与导航入口已补齐：
  - `/risk-monitoring`
  - `/risk-monitoring-gis`

### 2.2.1 当前阻塞与复验前置
- 2026-03-16 已按 `spring-boot-iot-admin/src/main/resources/application-dev.yml` 指向的真实环境完成登录冒烟，`/api/auth/login` 已恢复可用并可正常获取 token。
- `/api/risk-monitoring/realtime/list`、`/api/risk-monitoring/gis/points` 已确认接口路径与鉴权链路正确，且统一保持 `/api` 前缀不变。
- 当前共享开发库仍存在 schema 漂移：
  - 缺少 `risk_point_device` 表或关键列。
  - 旧版 `risk_point` 仍缺少 Phase 4 对齐字段 `create_by` / `update_by`。
- 复验前必须执行 `sql/upgrade/20260316_phase4_task3_risk_monitoring_schema_sync.sql`，完成后再进行真实环境联调。

### 2.3 明确未交付项
- GIS SDK / 第三方地图底图能力未交付。
- GIS 热力图、地图底图交互、驾驶舱内嵌风险监测子模块未交付。
- 首页驾驶舱已完成商业化重构，并升级为“数据看板驾驶舱 + 事务工作台入口”双层结构，支持一线/运维/管理/研发四类角色视角；当前仍采用静态能力编排与本地 activity 兜底，不纳入真实环境验收；设备中心增强仍未开始。

## 3. 当前轮次必须持续推进的工作项

### 3.1 P0 质量与验收
- 维护真实环境验收口径，不再新增端到端验收入口。
- 持续补齐 `docs/test-scenarios.md`、`docs/14-mqttx-live-runbook.md` 等真实环境验收文档。
- 启动并执行 Phase 4 真实环境验收，保留接口、页面、数据库、Redis、MQTTX 证据。

### 3.2 P0 数据一致性
- 持续推进 `iot_message_log` 命名统一方案。
- 所有新文档、新接口说明、新验收口径一律优先使用 `iot_message_log`。
- 历史实现仍使用旧表名时，必须显式标注兼容关系，避免新开发继续扩散旧命名。

### 3.3 P1 风险监测联调
- 使用真实环境数据完成实时监测列表联调。
- 使用真实环境数据完成 GIS 点位联调。
- 校验详情抽屉中的 24h 趋势、最近告警、最近事件三类数据可用性。
- 补齐空态、无经纬度数据、接口异常三类兜底验收。

### 3.4 P1 文档回写
- 只要接口、页面结构、验收方式、环境变量发生变化，必须同步回写 `docs/`。
- `README.md` 与 `AGENTS.md` 如无新增行为差异，可保持现状；如新增启动口径或验收脚本，再同步更新。

## 4. 后续里程碑建议

- M1：风险监测真实环境联调完成。
- M2：Phase 4 核心模块真实环境验收闭环完成。
- M3：`iot_message_log` 命名兼容/迁移方案落地。
- M4：首页商业化重构完成并稳定维护，设备中心增强进入开发。

## 5. 持续跟进任务清单

| 优先级 | 模块 | 任务 | 当前状态 | 产出 | 验收点 |
|---|---|---|---|---|---|
| P0 | 质量保障 | Phase 4 真实环境验收清单与脚本整理 | 进行中 | `docs/test-scenarios.md`、验收脚本、核对步骤 | 关键场景可按文档执行 |
| P0 | 数据一致性 | `iot_message_log` 命名统一方案 | 进行中 | 兼容或迁移方案 | 不影响已验证主链路 |
| P1 | 风险监测 | 实时监测 API 复验 | 数据库脚本阻塞待复验 | `/api/risk-monitoring/realtime/list` | 已执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql` 且接口返回非 SQL 500 |
| P1 | 风险监测 | 详情抽屉 API 复验 | 数据库脚本阻塞待复验 | `/api/risk-monitoring/realtime/{bindingId}` | 24h 趋势、最近告警、最近事件可查询 |
| P1 | 风险监测 | 实时监测页联调 | 开发完成待真实环境复验 | `RealTimeMonitoringView` | 列表、筛选、详情抽屉可用 |
| P1 | GIS | GIS 点位 API 复验 | 数据库脚本阻塞待复验 | `/api/risk-monitoring/gis/points` | 已执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql` 且接口返回点位数据 |
| P1 | GIS | GIS 页面联调 | 开发完成待真实环境复验 | `RiskGisView` | 点位渲染、未定位列表、详情抽屉可用 |
| P2 | 驾驶舱 | 首页信息架构商业化重构 | 已完成 | `CockpitView` 首页改版 | 去除无关入口，并围绕业务闭环组织首页 |
| P2 | 驾驶舱 | 首页角色视角与待办引导 | 已完成 | 角色 preset、经营入口卡片、待办中心与 activity 痕迹 | 首屏不依赖 `/api/cockpit/*` 真实聚合接口 |
| P2 | 设备中心 | 产品物模型增强 | 未开始 | 物模型管理页 | 支持风险监测字段 |
| P2 | 设备中心 | 设备管理扩展 | 未开始 | 设备列表增强 | 风险点与运行状态可见 |

## 6. 风险监测模块实现口径

### 6.1 页面拆分
- 实时监测与 GIS 风险态势必须保持两条独立路由，不做单页双 Tab。
- 详情统一使用右侧抽屉，不新增独立详情路由。
- `Cockpit` 只保留跳转入口，不承接本轮风险监测实现。

### 6.2 实时监测页要求
- 路由：`/risk-monitoring`
- 筛选项：区域、风险点、设备编码、风险等级、在线状态
- 列表字段：设备编码、设备名称、产品名称、风险点、测点、当前值、状态、最新上报时间、风险等级、告警标记
- 交互：点击“查看详情”打开统一详情抽屉

### 6.3 GIS 页要求
- 路由：`/risk-monitoring-gis`
- 可视化：基于 ECharts 的经纬度散点态势图，不引入第三方地图 SDK
- 编码规则：颜色映射风险等级，点大小映射活跃告警数
- 兜底要求：无经纬度数据的风险点必须进入“未定位风险点”列表
- 当前轮次不承诺底图、瓦片服务、热力图或 GIS SDK 能力

### 6.4 详情抽屉要求
- 展示当前监测信息
- 展示最近 24h 趋势
- 展示最近告警
- 展示最近事件
- 支持从列表页与 GIS 页复用

## 7. 验收与回归

- 构建：`mvn -s .mvn/settings.xml clean package -DskipTests`
- 后端运行：`mvn -s .mvn/settings.xml -pl spring-boot-iot-admin spring-boot:run -Dspring-boot.run.profiles=dev`
- 前端运行：`cd spring-boot-iot-ui && npm install && npm run acceptance:dev`
- 真实环境验收参考：`docs/test-scenarios.md`、`docs/14-mqttx-live-runbook.md`、`docs/04-api.md`

## 8. 更新记录

| 日期 | 更新内容 | 影响模块 | 责任人 | 备注 |
|---|---|---|---|---|
| 2026-03-16 | 确认本文件为 Phase 4 唯一权威进度文档 | 全局 | - | 后续冲突以本文件为准 |
| 2026-03-16 | 风险监测前端状态修正为“已完成开发待真实环境验证” | UI / alarm | Codex | GIS SDK 与底图能力仍未交付 |
| 2026-03-16 | 验收口径统一切换到真实环境，停止使用旧端到端验收链路 | 全局 | Codex | 参考 `application-dev.yml` 环境变量 |
| 2026-03-16 | 报表分析接口改为真实业务表聚合，并固定前端日期参数为 YYYY-MM-DD | report / UI | Codex | ReportServiceImpl 不再是连通性占位实现 |
| 2026-03-16 | 新增风险监测 schema 对齐脚本并补充共享开发库阻塞说明，明确需先执行 `20260316_phase4_task3_risk_monitoring_schema_sync.sql` 后再复验 | alarm / SQL / docs | Codex | 阻塞项集中在 `/api/risk-monitoring/*` |
| 2026-03-16 | 首页驾驶舱完成商业化重构，并补齐角色视角 preset 与待办中心，当前仍不纳入真实环境验收 | UI | Codex | 首页主视图采用静态能力编排与 activity 兜底 |
| 2026-03-17 | 系统治理分页基线补齐：用户、角色、字典、通知渠道改为真实后端分页；组织、区域、菜单改为“根节点分页 + 子节点懒加载”；审计日志分页改为数据库分页 | system / UI / docs | Codex | 解决系统治理页前端假分页与后端全量查询问题 |
| 2026-03-17 | 系统治理分页工程化补强：新增 `PageQueryUtils`、`useServerPagination` 与历史库分页索引脚本，统一前后端分页收口并降低真实库分页查询成本 | framework / system / UI / SQL / docs | Codex | 覆盖分页参数归一化、分页状态复用与历史库索引补齐 |
| 2026-03-18 | 日志能力收敛优化：新增业务审计统计接口，业务日志/系统日志统一补齐 `operationResult` 筛选和统计概览，明确“业务审计 + 后端排障”双目标 | system / UI / docs | Codex | 避免建设目标发散，优先增强现有日志链路可用性 |
| 2026-03-18 | 公共壳层继续商业化收口：顶部导航、左侧菜单和内容页头统一按阿里云式浅色控制台重组，提升导航层级和页面定位的一致性 | UI / docs | Codex | 不改业务路由与权限，仅收敛展示规范与信息架构 |

## 日志收敛补充（2026-03-18）
- 已补齐“系统异常到消息追踪”的最小链路兜底：当 MQTT 消息在分发前置校验阶段失败时，补写 iot_device_message_log 追踪记录（messageType=dispatch_failed）。
- 目标保持不变：业务日志服务审计，系统日志服务后端排障；本次仅增强链路可追溯性，不扩散到其他建设范围。
