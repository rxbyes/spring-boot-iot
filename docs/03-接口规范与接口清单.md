# 03 接口规范与接口清单

> 文档定位：对外接口主索引，统一鉴权、分页、TraceId 和模块接口口径。
> 适用角色：后端、前端、测试、运维、联调人员。
> 权威级别：一级权威。
> 上游来源：Controller 源码、DTO/VO、运行配置、当前真实环境口径。
> 下游消费：前后端联调、帮助中心技术稿、真实环境验收、日志排障。
> 变更触发条件：接口新增删除、路径变化、鉴权规则变化、系统内容接口变化、日志查询口径变化。
> 更新时间：2026-03-27

本清单以 Controller 源码为准，目标是作为二次开发与联调的主索引。

职责边界：
- 本文件只负责对外接口口径、鉴权、分页、TraceId 与 OpenAPI/SDK 发布说明。
- 测试策略与质量门禁以 [05-自动化测试与质量保障.md](./05-自动化测试与质量保障.md) 为准。
- 真实环境执行步骤与留痕以 [真实环境测试与验收手册.md](./真实环境测试与验收手册.md) 为准。
- 交付能力矩阵与验收判定以 [21-业务功能清单与验收标准.md](./21-业务功能清单与验收标准.md) 为准。

## 1. 通用规范

### 1.1 基础协议

- 接口风格：REST + JSON。
- 统一返回：`R`，典型格式：

```json
{
  "code": 200,
  "msg": "success",
  "data": {}
}
```

- 业务错误：`code != 200`，前端统一按失败处理。

### 1.2 鉴权规范

- 默认要求：`Authorization: Bearer <token>`。
- 公共白名单：
  - `POST /api/auth/login`
  - `POST /api/message/http/report`
  - `/api/cockpit/**`
  - `/actuator/**`
  - `/doc.html`
  - `/swagger-ui/**`
  - `/swagger-ui.html`
  - `/v3/api-docs/**`

### 1.3 链路追踪

- 请求/响应可携带 `X-Trace-Id`。
- `/api/*` 会注入或透传 traceId。

### 1.4 分页约定

【根据代码推断】多数分页接口采用 `pageNum`、`pageSize`，返回 `PageResult`（`records`、`total`、`pageNum`、`pageSize`）。

### 1.5 OpenAPI 与 SDK 生成发布口径

- 当前运行时 OpenAPI 入口继续使用：
  - `/v3/api-docs/**`
  - `/swagger-ui/**`
  - `/swagger-ui.html`
  - `/doc.html`
- 当前权威顺序为：
  1. Controller 与 DTO / VO 源码
  2. 本文件中的接口口径与兼容说明
  3. 运行时 OpenAPI 文档
- 当前仓库未见“自动生成 SDK 并发布到制品库”的独立流水线配置，因此：
  - OpenAPI 可作为联调和生成 SDK 的输入源；
  - 但生成产物不是新的权威源，不能反向覆盖本文件和代码事实。
- 若后续要对外发布 SDK，至少需要同步记录：
  - 生成日期与对应分支 / 提交
  - 使用的 `basePath`
  - 覆盖的模块范围
  - 是否包含未纳入当前交付范围的灰度接口
- 风险监测、非 Reactor 模块或灰度接口如需出现在 SDK 中，必须先与 [19-第四阶段交付边界与复验进展.md](./19-第四阶段交付边界与复验进展.md) 和 [21-业务功能清单与验收标准.md](./21-业务功能清单与验收标准.md) 的交付口径对齐。

## 2. 认证接口（auth）

- `POST /api/auth/login`：账号登录。
- `GET /api/auth/me`：当前登录用户信息。

## 3. 设备接入与调试接口（device/message）

### 3.1 产品与设备

- `POST /api/device/product/add`
- `GET /api/device/product/page`
- `GET /api/device/product/list`
- `GET /api/device/product/{id}`
- `PUT /api/device/product/{id}`
- `DELETE /api/device/product/{id}`
- `GET /api/device/product/{productId}/models`
- `POST /api/device/product/{productId}/models`
- `PUT /api/device/product/{productId}/models/{modelId}`
- `DELETE /api/device/product/{productId}/models/{modelId}`
- `POST /api/device/add`
- `POST /api/device/batch-add`
- `GET /api/device/{id}`
- `GET /api/device/code/{deviceCode}`
- `GET /api/device/page`
- `PUT /api/device/{id}`
- `POST /api/device/{id}/replace`
- `DELETE /api/device/{id}`
- `POST /api/device/batch-delete`
- `GET /api/device/list`
- `GET /api/device/{deviceId}/metrics`

补充口径：
- `POST /api/device/product/add` 与 `PUT /api/device/product/{id}` 当前共用 `ProductAddDTO`，必填字段为 `productKey`、`productName`、`protocolCode`、`nodeType`，可选字段为 `dataFormat`、`manufacturer`、`description`、`status`。
- `productKey` 是产品的机器侧唯一标识；【根据代码推断】当前后端按 `(tenant_id, product_key)` 做唯一约束，且更新产品时若试图修改 `productKey` 会直接返回业务异常。
- `POST /api/device/add`、`PUT /api/device/{id}` 与 `POST /api/device/batch-add` 当前都要求传入 `productKey`；后端会先按 `productKey` 查产品，再把产品的 `protocolCode`、`nodeType` 继承到设备。
- `POST /api/device/add`、`PUT /api/device/{id}` 与 `POST /api/device/{id}/replace` 当前支持 `parentDeviceId`（父设备主键）与 `parentDeviceCode`（父设备编码，二者不可同时填写）；批量导入建议优先使用 `parentDeviceCode`。
- 当目标设备产品的 `nodeType=3`（网关子设备）且已选择父设备时，后端会按父设备链路自动回填 `gatewayId`；详情、分页结果和设备选项接口都会同步返回父设备与关联网关的展示字段。
- `DELETE /api/device/product/{id}` 当前会在删除前校验关联设备数量，若仍存在库存设备会拒绝删除。
- `GET /api/device/product/list` 当前只返回“未删除且启用中”的产品，供设备建档和前端下拉框使用。
- `GET /api/device/product/{id}` 当前除基础产品档案外，还会返回 `todayActiveCount`、`sevenDaysActiveCount`、`thirtyDaysActiveCount`、`avgOnlineDuration`、`maxOnlineDuration` 五个详情增强字段。
- `GET /api/device/product/{productId}/models` 当前按产品维度返回物模型列表，稳定排序规则为 `sortNo asc, identifier asc`。
- `GET /api/device/product/{productId}/model-candidates` 当前返回 `ProductModelCandidateResult`：包含 `summary`、`propertyCandidates`、`eventCandidates`、`serviceCandidates`，用于 `/products` 内抽屉展示“真实证据概览 + 候选目录 + 候选列表”。
- 属性候选当前优先从 `iot_device_property` 聚合，并辅以 `iot_device_message_log` 最近 30 天的 `property / status` 消息补充最近上报时间和消息证据；`groupKey` 会按 `telemetry / device_status / location / unknown` 分类，且会输出建议 `modelName`、中文 `description`、`confidence`、`needsReview`。
- `eventCandidates` 与 `serviceCandidates` 当前坚持真实证据优先：只有在 `iot_device_message_log` 或 `iot_command_record` 存在稳定证据时才生成候选；若无证据，接口返回空列表并通过 `summary.eventHint / serviceHint` 给出诚实提示。
- `POST /api/device/product/{productId}/models` 与 `PUT /api/device/product/{productId}/models/{modelId}` 当前共用 `ProductModelUpsertDTO`，字段包括 `modelType`、`identifier`、`modelName`、`dataType`、`specsJson`、`eventType`、`serviceInputJson`、`serviceOutputJson`、`sortNo`、`requiredFlag`、`description`。
- `POST /api/device/product/{productId}/model-candidates/confirm` 当前接收 `items[]`：每项包含待确认候选的 `modelType`、`identifier`、`modelName`、`dataType`、`eventType`、`serviceInputJson`、`serviceOutputJson`、`sortNo`、`requiredFlag`、`description`；服务端只写入确认项，并对已存在 `identifier` 返回 `createdCount / skippedCount / conflictCount`。
- `modelType` 当前只允许 `property`、`event`、`service`：
  - `property` 只允许填写 `dataType` 与可选 `specsJson`
  - `event` 只允许填写 `eventType`
  - `service` 只允许填写 `serviceInputJson`、`serviceOutputJson`
- `specsJson`、`serviceInputJson`、`serviceOutputJson` 当前都在服务层执行 JSON 合法性校验；更新与删除时都会校验 `modelId` 属于当前 `productId`。
- 受真实库 `iot_product_model.data_type NOT NULL` 约束影响，`event` / `service` 当前在落库时会内部写入兼容值 `json`；但 `GET/POST/PUT /api/device/product/{productId}/models*` 对外仍保持 `dataType=null`，前端无需补填该字段。
- 同一 `productId` 下 `identifier` 当前必须唯一；控制器路径固定挂在产品资源下，不新增顶层菜单接口。
- 当前真实库 `iot_command_record` 仍可能缺少新版 `service_identifier` 等列；当服务证据字段未对齐时，`GET /model-candidates` 会退化为“空服务候选 + 明确提示”，而不是返回伪候选或直接抛前端不可用错误。
- 活跃设备数口径基于 `iot_device.last_report_time` 的自然日窗口聚合。
- `avgOnlineDuration`、`maxOnlineDuration` 当前基于 `iot_device_online_session` 聚合，统计近 30 天内会话的平均/最大在线分钟数。
- 在线会话结束时间当前优先按 `iot_device.last_online_time + iot.device.online-timeout-seconds` 推断；若历史数据尚未补齐 `last_online_time`，则回退到 `last_report_time + iot.device.online-timeout-seconds`。当前默认超时阈值为 `7200` 秒。
- 在线时长不回填历史老会话，若产品在会话表中暂无记录，则这两个字段返回 `null`。
- `PUT /api/device/product/{id}` 当前在把产品状态改为停用时，会先校验该产品下是否仍存在启用中的关联设备。
- 停用产品当前会被后端拒绝用于 `POST /api/device/add`、`POST /api/device/batch-add`、设备更换建档、设备上报接入，以及 `POST /api/message/mqtt/down/publish` 下行指令下发。
- `POST /api/device/add`、`POST /api/device/batch-add` 与 `POST /api/device/{id}/replace` 成功后，当前会自动把同 `productKey + deviceCode` 的 `DEVICE_NOT_FOUND` 无效上报最新态标记为 `resolved=1`，避免未登记名单继续保留已补录设备的历史噪音。
- `GET /api/device/page` 当前新增可选筛选参数 `registrationStatus`：`1` 只返回已登记设备，`0` 只返回未登记上报名单，空值则在同一分页结果内先返回已登记设备，再按剩余页容量补入未登记设备。
- `GET /api/device/page` 返回项当前新增 `registrationStatus`、`assetSourceType`、`sourceRecordId`、`lastFailureStage`、`lastErrorMessage`、`lastReportTopic`、`lastTraceId`、`lastPayload`；其中未登记行 `id=null`、`registrationStatus=0`，仅用于列表展示与详情排障。
- 未登记名单当前优先来源于 `iot_device_invalid_report_state` 中 `reasonCode=DEVICE_NOT_FOUND` 且 `resolved=0` 的最新态记录；若共享环境尚未补齐该表或列结构，则回退到 `iot_device_access_error_log` 最近一条失败样本，再回退到仅存在 `iot_device_message_log(messageType=dispatch_failed)` 的最近失败轨迹。
- `GET /api/device/page` 在未登记名单场景下，当前只支持 `productKey`、`deviceCode` 与 `registrationStatus` 过滤；若同时传入 `deviceId`、`deviceName`、`onlineStatus`、`activateStatus`、`deviceStatus`，未登记名单会按“无命中”处理，不伪造运行态字段筛选结果。
- `GET /api/device/list` 默认只返回启用中的设备选项；当传入 `includeDisabled=true` 时，也会返回禁用设备，供设备资产中心在编辑/更换场景回显现有父设备关系。
- `/devices` 打开未登记详情时，只有 `assetSourceType=access_error` 的行才保证可通过 `GET /api/device/access-error/{id}` 补全失败归档详情；若当前行来自 `invalid_report_state` 或 `dispatch_failed` 来源，则页面继续以列表摘要为准，不额外伪造不存在的失败归档明细。

### 3.2 上报与下发

- `POST /api/message/http/report`
- `POST /api/message/mqtt/report/publish`
- `POST /api/message/mqtt/down/publish`
  - 若目标设备所属产品已停用，后端会直接返回业务错误，拒绝继续下发。

补充口径：
- 链路验证中心当前已按“设备编码 -> 查询设备 -> 发送模拟”交互收口：前端先通过 `GET /api/device/code/{deviceCode}` 反查设备接入契约，再只读回显 `productKey`、`protocolCode`、`clientId=deviceCode`；`tenantId` 默认 `1` 且仅 HTTP 生效，`topic` 默认 `$dp` 且允许手工修改。
- `POST /api/message/http/report` 当前支持链路验证中心双模式：
  - 明文模式：只有可识别 JSON 才按 C.1/C.2/C.3 自动构造“类型字节 + 大端长度 + 正文/文件流”二进制帧。
  - 明文 XML / 普通文本：不做帧识别，直接按原始文本透传。
  - 密文模式：前端直接透传 `{"header":{"appId":"..."},"bodies":{"body":"..."}}` 封包。
- `POST /api/message/http/report` 当前返回 `R<MessageFlowSubmitResultVO>`，`data` 固定字段：
  - `sessionId`
  - `traceId`
  - `status`
  - `timelineAvailable`
  - `correlationPending`
- 当深部位移子设备已绑定 `risk_point_device` 且 `iot.alarm.auto-closure.enabled=true` 时，`POST /api/message/http/report` / MQTT 上报在完成 `$dp -> 解密/解析 -> 子设备映射 -> 属性落库` 后，会继续自动执行红橙黄蓝判级、告警/事件/工单闭环；该能力不新增对外 API 路径。
- 自动闭环命中应急预案时，会优先从启用预案中筛选名称/描述/响应步骤/联系人文本里能命中深部位移场景关键词或风险点上下文的记录；若无场景化预案，留痕允许为空。
- 自动闭环内部会额外使用 Redis 分布式锁保护同一 `deviceCode + metricIdentifier + level` 的并发去重；该治理不改变现有接口入参、出参和响应结构。
- 新增可选字段 `payloadEncoding`：
  - 默认或空值：按 `UTF-8` 取字节（兼容历史调用）。
  - `ISO-8859-1` / `latin1`：按单字节映射取字节，适用于明文二进制帧模拟。
- `POST /api/message/mqtt/report/publish` 为“原始 MQTT 上行模拟发布”接口，请求体固定字段如下：
  - 必填：`deviceCode`、`productKey`、`protocolCode`、`topic`、`payload`
  - 可选：`payloadEncoding`、`qos`、`retained`
  - 发送语义：直接把原始字节发布到 Broker，再由现有 MQTT consumer 回流进入主链路；不复用 `DownMessageService` 下行编码逻辑。
  - 发送前校验：MQTT 客户端已连接、设备存在、设备所属产品与传入 `productKey` 一致；协议校验优先使用设备协议，设备协议为空时回退到产品协议；若仍为空则返回“目标设备未配置协议”，若不一致则返回 `expected/actual` 明细。
  - 前端兼容规则：当传输方式为 MQTT、`topic=$dp`、明文内容为普通属性 JSON 且未携带设备身份字段时，前端会先补入 `deviceCode` 再调用该接口。
  - 返回 `R<MessageFlowSubmitResultVO>`，`data` 固定字段：
    - `sessionId`
    - `traceId`（发布成功时允许为空，待消费回流绑定）
    - `status=PUBLISHED`
    - `timelineAvailable=false`
    - `correlationPending=true`
- MQTT consumer 当前已启用首批无效上报治理，但不改变接口出入参契约：对 `DEVICE_NOT_FOUND` 与 `EMPTY_DECRYPTED_PAYLOAD` 会先采样、后进入冷却抑制；其中原始空 payload 会在 consumer 入口直接丢弃，冷却期内重复坏包不再保证继续写入 `dispatch_failed`、`iot_device_access_error_log` 或 `sys_audit_log`。

调试示例：

```json
{
  "protocolCode": "mqtt-json",
  "productKey": "demo-product",
  "deviceCode": "demo-device-01",
  "payload": "{\"messageType\":\"property\",\"properties\":{\"temperature\":26.5,\"humidity\":68}}",
  "topic": "/sys/demo-product/demo-device-01/thing/property/post",
  "clientId": "demo-device-01",
  "tenantId": "1"
}
```

```bash
curl -X POST http://localhost:9999/api/message/http/report \
  -H "Content-Type: application/json" \
  -d '{
    "protocolCode":"mqtt-json",
    "productKey":"demo-product",
    "deviceCode":"demo-device-01",
    "payload":"{\"messageType\":\"property\",\"properties\":{\"temperature\":26.5,\"humidity\":68}}",
    "topic":"/sys/demo-product/demo-device-01/thing/property/post",
    "clientId":"demo-device-01",
    "tenantId":"1"
  }'
```

### 3.3 属性、日志与追踪

- `GET /api/device/{deviceCode}/properties`
- `GET /api/device/{deviceCode}/message-logs`
- `GET /api/device/message-trace/page`
- `GET /api/device/message-trace/stats`
- `GET /api/device/message-flow/session/{sessionId}`
- `GET /api/device/message-flow/trace/{traceId}`
- `GET /api/device/message-flow/ops/overview`
- `GET /api/device/message-flow/recent`
- `GET /api/device/access-error/page`
- `GET /api/device/access-error/stats`
- `GET /api/device/access-error/{id}`
- `GET /api/telemetry/latest`
- `GET /api/device/{deviceCode}/file-snapshots`
- `GET /api/device/{deviceCode}/firmware-aggregates`
- `GET /actuator/health/mqttConsumer`

补充口径：
- `GET /api/device/message-trace/stats` 当前返回追踪聚合摘要，至少包含 `total`、`recentHourCount`、`recent24HourCount`、`distinctTraceCount`、`distinctDeviceCount`、`dispatchFailureCount`，以及 `topMessageTypes / topProductKeys / topDeviceCodes / topTopics`。
- `GET /api/device/message-flow/session/{sessionId}` 当前返回 `MessageFlowSessionVO`，固定字段：
  - `sessionId`
  - `transportMode`
  - `status`
  - `submittedAt`
  - `traceId`
  - `deviceCode`
  - `topic`
  - `correlationPending`
  - `timeline`
- `GET /api/device/message-flow/trace/{traceId}` 当前返回 `MessageFlowTimelineVO`，固定字段：
  - `traceId`
  - `sessionId`
  - `flowType`
  - `status`
  - `deviceCode`
  - `productKey`
  - `topic`
  - `protocolCode`
  - `messageType`
  - `startedAt`
  - `finishedAt`
  - `totalCostMs`
  - `steps`
- `steps` 当前固定字段：
  - `stage`
  - `handlerClass`
  - `handlerMethod`
  - `status`
  - `costMs`
  - `startedAt`
  - `finishedAt`
  - `summary`
  - `errorClass`
  - `errorMessage`
  - `branch`
- `message-flow` 时间线当前固定阶段顺序为 `INGRESS -> TOPIC_ROUTE -> PROTOCOL_DECODE -> DEVICE_CONTRACT -> MESSAGE_LOG -> PAYLOAD_APPLY -> TELEMETRY_PERSIST -> DEVICE_STATE -> RISK_DISPATCH -> COMPLETE`。
- `TELEMETRY_PERSIST` 步骤当前固定汇总 `persistedTargetCount`、`persistedPointCount`、`skippedTargetCount`、`failedTargetCount`，并补充 `storageMode`、`legacyStableCount`、`legacyColumnCount`、`normalizedFallbackCount`、`legacyMappedMetricCount`、`legacyUnmappedMetricCount`、`fallbackMetricCount`、`fallbackReasons`、`skippedMetricCount`；当 dev / prod 基线启用 telemetry v2 时，`branch` 会落成 `TDENGINE_V2_RAW`、`storageMode=tdengine-v2`，legacy / fallback 计数按异步镜像与兼容回退结果分别保留为 `0` 或实际值；TDengine 写失败时步骤会返回 `FAILED/NON_BLOCKING_FAILURE`，但整个 session 仍可 `COMPLETED`。
- `GET /api/device/message-flow/ops/overview` 当前返回 `MessageFlowOpsOverviewVO`，固定字段：
  - `runtimeStartedAt`
  - `sessionCounts`
  - `correlationCounts`
  - `lookupCounts`
  - `stageMetrics`
- `stageMetrics` 当前固定字段：
  - `stage`
  - `count`
  - `failureCount`
  - `skippedCount`
  - `avgCostMs`
  - `p95CostMs`
  - `maxCostMs`
- `GET /api/device/message-flow/recent` 当前查询参数固定为 `size`、`deviceCode`、`topic`、`transportMode`、`status`，返回 `MessageFlowRecentSessionVO[]`，固定字段：
  - `sessionId`
  - `traceId`
  - `transportMode`
  - `status`
  - `submittedAt`
  - `deviceCode`
  - `topic`
  - `correlationPending`
  - `timelineAvailable`
- `message-flow` 时间线默认只保留 Redis 短期数据；若超出 TTL，接口允许返回 `data=null`，前端按“时间线已过期，仅保留消息日志”降级展示。
- `message-flow` 查询接口当前保持主体响应形态不变，不新增 lookup envelope；当 `session/trace` 不存在或 Redis 中已过期时，统一允许 `data=null`，缺失原因由前端按 `/reporting`、`/message-trace` 页面上下文推断。
- `GET /api/device/access-error/page`、`GET /api/device/access-error/{id}` 当前依赖 `iot_device_access_error_log`；历史库若缺表，需先执行 `sql/upgrade/20260322_phase5_device_access_error_archive.sql` 或 `python scripts/run-real-env-schema-sync.py`。
- `iot_device_access_error_log` 当前定位为“失败样本归档表”：只保留首批命中或冷却期外重采样的失败详情，不再保证每条重复坏包都插一行。
- `GET /api/device/access-error/stats` 当前返回失败归档聚合摘要，至少包含 `total`、`recentHourCount`、`recent24HourCount`、`distinctTraceCount`、`distinctDeviceCount`，以及 `topFailureStages / topErrorCodes / topExceptionClasses / topProtocolCodes / topTopics`；其中未带筛选条件时，`recentHourCount`、`recent24HourCount` 与 `topFailureStages` 改读 Redis `invalid-report` 分钟桶聚合，`topErrorCodes / topExceptionClasses / topProtocolCodes / topTopics` 仍来自样本归档表。
- 失败归档详情当前会额外返回 `contractSnapshot`，固定包含 `routeType`、`expectedProtocolCode`、`actualProtocolCode`、`protocolSource`、`deviceProtocolCode`、`productProtocolCode`、`deviceProductId`、`resolvedProductId`、`productKey`。
- MQTT `$dp` 分发失败时，系统异常审计的 `request_params` 当前会尽量补齐解码后的 `deviceCode`、`productKey`、`protocolCode`、`clientId`、`topicRouteType`，便于直接做接入契约对比。
- `GET /actuator/health/mqttConsumer` 当前返回 `running`、`connected`、`clientId`、`brokerUrl`、`subscribeTopics`、`lastDisconnectAt` 以及最近一次连接/收包/成功分发/失败阶段摘要，供真实环境回放前先确认 consumer 是否在线。
- 规则化运维告警本阶段不新增 REST 接口，继续复用 `sys_audit_log`、Redis `invalid-report` 分钟桶、`iot_device_access_error_log` 样本归档、`/actuator/health/mqttConsumer` 与通知渠道 `scenes` 做调度评估和通知分发。

### 3.4 驾驶舱接口（公共）

- `GET /api/cockpit/data`
- `GET /api/cockpit/trend`
- `GET /api/cockpit/distribution`
- `GET /api/cockpit/warnings`
- `GET /api/cockpit/activities`

## 4. 风险预警与处置接口（alarm/report）

### 4.1 告警中心 `/api/alarm`

- `POST /api/alarm/add`
- `GET /api/alarm/list`
- `GET /api/alarm/{id}`
- `POST /api/alarm/{id}/confirm`
- `POST /api/alarm/{id}/suppress`
- `POST /api/alarm/{id}/close`

补充口径：
- 深部位移自动闭环生成的告警同样通过 `GET /api/alarm/list`、`GET /api/alarm/{id}` 查询，无单独接口。
- 自动闭环当前写入四级 `alarmLevel`：`critical/high/medium/low`；联动规则命中摘要与 `planId` 写入 `iot_alarm_record.remark`，无场景化预案时 `planId` 允许为空。
- 若共享环境同时存在未升级旧版 consumer，同一条 MQTT 上报仍可能生成旧口径告警留痕；这属于环境混跑，不属于当前接口契约回退。

### 4.2 事件处置 `/api/event`

- `POST /api/event/add`
- `GET /api/event/list`
- `GET /api/event/{id}`
- `POST /api/event/{id}/dispatch`
- `POST /api/event/{id}/close`
- `POST /api/event/{eventId}/feedback`
- `GET /api/event/work-orders`
- `POST /api/event/work-orders/{id}/receive`
- `POST /api/event/work-orders/{id}/start`
- `POST /api/event/work-orders/{id}/complete`

补充口径：
- 深部位移自动闭环当前仅在橙色、红色场景下自动创建事件，并立即调用既有派发接口能力生成工单。
- 自动闭环事件的命中留痕写入 `iot_event_record.review_notes`，不新增表、不新增接口；若无场景化应急预案，`review_notes.emergencyPlan` 允许为空。

### 4.3 风险点 `/api/risk-point`

- `POST /api/risk-point/add`
- `POST /api/risk-point/update`
- `POST /api/risk-point/delete/{id}`
- `GET /api/risk-point/get/{id}`
- `GET /api/risk-point/list`
- `GET /api/risk-point/page`
- `POST /api/risk-point/bind-device`
- `POST /api/risk-point/unbind-device`
- `GET /api/risk-point/bound-devices/{riskPointId}`

### 4.4 阈值规则 `/api/rule-definition`

- `GET /api/rule-definition/list`
- `GET /api/rule-definition/page`
- `GET /api/rule-definition/get/{id}`
- `POST /api/rule-definition/add`
- `POST /api/rule-definition/update`
- `POST /api/rule-definition/delete/{id}`

### 4.5 联动规则 `/api/linkage-rule`

- `GET /api/linkage-rule/list`
- `GET /api/linkage-rule/page`
- `GET /api/linkage-rule/get/{id}`
- `POST /api/linkage-rule/add`
- `POST /api/linkage-rule/update`
- `POST /api/linkage-rule/delete/{id}`

### 4.6 应急预案 `/api/emergency-plan`

- `GET /api/emergency-plan/list`
- `GET /api/emergency-plan/page`
- `GET /api/emergency-plan/get/{id}`
- `POST /api/emergency-plan/add`
- `POST /api/emergency-plan/update`
- `POST /api/emergency-plan/delete/{id}`

### 4.7 风险监测（待验收）`/api/risk-monitoring`

- `GET /api/risk-monitoring/realtime/list`
- `GET /api/risk-monitoring/realtime/{bindingId}`
- `GET /api/risk-monitoring/gis/points`

说明：风险监测接口已在代码中实现，但交付口径仍需以真实环境验收结果确认。

### 4.8 报表接口 `/api/report`

- `GET /api/report/risk-trend`
- `GET /api/report/alarm-statistics`
- `GET /api/report/event-closure`
- `GET /api/report/device-health`

## 5. 系统治理接口（system）

### 5.1 用户 `/api/user`

- `POST /api/user/add`
- `GET /api/user/list`
- `GET /api/user/page`
- `GET /api/user/{id}`
- `PUT /api/user/update`
- `DELETE /api/user/{id}`
- `GET /api/user/username/{username}`
- `POST /api/user/change-password`
- `POST /api/user/reset-password/{userId}`

### 5.2 角色 `/api/role`

- `POST /api/role/add`
- `GET /api/role/list`
- `GET /api/role/page`
- `GET /api/role/{id}`
- `PUT /api/role/update`
- `DELETE /api/role/{id}`
- `GET /api/role/user/{userId}`

### 5.3 菜单 `/api/menu`

- `GET /api/menu/tree`
- `GET /api/menu/list`
- `GET /api/menu/page`
- `GET /api/menu/{id}`
- `POST /api/menu/add`
- `PUT /api/menu/update`
- `DELETE /api/menu/{id}`

### 5.4 组织 `/api/organization`

- `POST /api/organization`
- `GET /api/organization/list`
- `GET /api/organization/page`
- `GET /api/organization/tree`
- `GET /api/organization/{id}`
- `PUT /api/organization`
- `DELETE /api/organization/{id}`

### 5.5 区域 `/api/region`

- `GET /api/region/list`
- `GET /api/region/page`
- `GET /api/region/tree`
- `GET /api/region/{id}`
- `POST /api/region`
- `PUT /api/region`
- `DELETE /api/region/{id}`

### 5.6 字典 `/api/dict`

- `GET /api/dict/list`
- `GET /api/dict/page`
- `GET /api/dict/tree`
- `GET /api/dict/{id}`
- `GET /api/dict/code/{dictCode}`
- `POST /api/dict`
- `PUT /api/dict`
- `DELETE /api/dict/{id}`

### 5.7 通知渠道 `/api/system/channel`

- `GET /api/system/channel/list`
- `GET /api/system/channel/page`
- `GET /api/system/channel/getByCode/{channelCode}`
- `POST /api/system/channel/add`
- `PUT /api/system/channel/update`
- `DELETE /api/system/channel/delete/{id}`
- `POST /api/system/channel/test/{channelCode}`

补充口径：
- 当前通知渠道测试接口仍统一发送一条通用测试消息，不区分 `system_error`、`observability_alert` 或 `in_app_unread_bridge` 场景。
- `webhook / wechat / feishu / dingtalk` 渠道配置 JSON 当前统一支持：
  - `url`
  - `headers`
  - `scenes`
  - `timeoutMs`
  - `minIntervalSeconds`
- `scenes` 当前已支持三个自动场景：
  - `system_error`：后台异常审计自动通知。
  - `observability_alert`：规则化运维告警评估器命中阈值后的外部通知。
  - `in_app_unread_bridge`：高优先级站内消息超过阈值仍未读时的桥接通知。

### 5.8 站内消息 `/api/system/in-app-message`

- 管理端：
  - `GET /api/system/in-app-message/page`
  - `GET /api/system/in-app-message/stats`
  - `GET /api/system/in-app-message/bridge/stats`
  - `GET /api/system/in-app-message/bridge/page`
  - `GET /api/system/in-app-message/bridge/{id}/attempts`
  - `GET /api/system/in-app-message/{id}`
  - `POST /api/system/in-app-message/add`
  - `PUT /api/system/in-app-message/update`
  - `DELETE /api/system/in-app-message/delete/{id}`
- 当前用户消费端：
  - `GET /api/system/in-app-message/my/page`
  - `GET /api/system/in-app-message/my/unread-count`
  - `GET /api/system/in-app-message/my/{id}`
  - `POST /api/system/in-app-message/my/read/{id}`
  - `POST /api/system/in-app-message/my/read-all`

补充口径：
- 消息类型当前固定为 `system / business / error`；优先级固定为 `critical / high / medium / low`。
- 来源类型当前固定收口为 `manual / system_error / event_dispatch / work_order / governance`；旧样例中的 `system_maintenance / daily_report / governance_task` 在升级后会分别回填到 `manual / manual / governance`。
- 推送范围当前固定为 `all / role / user`，分别通过 `target_type`、`target_role_codes`、`target_user_ids` 表达；当前先不引入单独的发布明细关系表。
- 管理端 `GET /api/system/in-app-message/page` 当前已支持 `sourceType` 筛选；`GET /api/system/in-app-message/stats` 当前返回发布时间范围、消息类型、来源类型、总发送量、已读量、未读量、已读率、趋势桶、来源占比和高未读消息榜单。
- `sys_in_app_message` 当前已新增 `dedup_key`；后端去重规则固定为 `sourceType + sourceId + target fingerprint + messageType`，自动消息默认通过服务层查重复用，不额外加数据库唯一约束。
- `GET /api/system/in-app-message/my/unread-count` 当前返回总未读数和按 `system / business / error` 分类的未读数，便于壳层通知中心直接显示摘要指标。
- 当前用户消息列表只返回“已发布、未删除、未过期且当前用户有权访问”的消息；已读状态由 `sys_in_app_message_read` 单独记录。
- 当前用户消息列表排序已固定为“未读优先 -> 优先级 -> 发布时间倒序”；壳层会在登录后、标签页重新激活、通知摘要/列表打开以及显式已读后立即刷新，并通过 `BroadcastChannel` / `storage` 事件同步多标签页未读徽标。
- 系统自动消息当前只允许查看或停用，不允许在管理端随意改正文或删除；手工广播与治理类消息仍可通过治理页新增、编辑、删除。
- 当前高优未读桥接规则：
  - 不新增对外 HTTP 接口，完全由后端调度任务在壳层消息闭环外部执行。
  - 仅扫描 `high / critical` 且超过阈值仍未读的消息。
  - 当前按“每条站内消息 + 每个渠道一次成功闭环、失败按 `minIntervalSeconds` 冷却重试”的策略执行，并把结果写入 `sys_in_app_message_bridge_log`。
- 桥接观测接口当前统一挂在管理端 `/api/system/in-app-message/bridge/*`：
  - `GET /api/system/in-app-message/bridge/stats`：参数支持 `startTime`、`endTime`、`messageType`、`sourceType`、`priority`、`channelCode`、`bridgeStatus`；时间口径固定过滤 `sys_in_app_message_bridge_log.last_attempt_time`，默认最近 `7` 天，若 `startTime > endTime` 直接返回业务错误。
  - `GET /api/system/in-app-message/bridge/page`：参数与统计接口一致，另加 `pageNum`、`pageSize`；返回 `PageResult<InAppMessageBridgeLogVO>`，字段固定为 `id`、`messageId`、`title`、`messageType`、`priority`、`sourceType`、`sourceId`、`relatedPath`、`publishTime`、`channelCode`、`channelName`、`channelType`、`bridgeScene`、`bridgeStatus`、`unreadCount`、`attemptCount`、`lastAttemptTime`、`successTime`、`responseStatusCode`、`responseBody`。
  - `GET /api/system/in-app-message/bridge/{id}/attempts`：返回 `List<InAppMessageBridgeAttemptVO>`，字段固定为 `id`、`bridgeLogId`、`messageId`、`channelCode`、`bridgeScene`、`attemptNo`、`bridgeStatus`、`unreadCount`、`recipientSnapshot`、`responseStatusCode`、`responseBody`、`attemptTime`，并按 `attemptNo desc` 返回。
- 桥接统计聚合当前固定基于 `sys_in_app_message_bridge_log` 联查 `sys_in_app_message` 与 `sys_notification_channel`：
  - `totalBridgeCount` = 汇总表筛选结果条数。
  - `successCount` = `bridge_status = 1`。
  - `pendingRetryCount` = `bridge_status = 0`。
  - `totalAttemptCount` = `sum(attempt_count)`。
  - `successRate` = `successCount / totalBridgeCount`。
  - `trend` 按 `DATE(last_attempt_time)` 聚合，`channelBuckets` 按 `channel_code` 聚合，`sourceTypeBuckets` 按消息 `source_type` 聚合。
- 桥接尝试明细当前由 `sys_in_app_message_bridge_attempt_log` 承载：
  - 每次桥接扫描落一条尝试记录，`attempt_no` 固定等于汇总表更新后的 `attempt_count`。
  - `bridge_status` 口径固定为 `1=本次桥接成功`、`0=本次桥接失败`。
  - `response_body` 继续沿用服务端 `1000` 字符截断口径，不额外持久化请求报文。
- 当前已接入的自动消息源：
  - `system_error`：来源于后台异常审计链路，目标角色固定为 `OPS_STAFF / DEVELOPER_STAFF / SUPER_ADMIN`，关联页面 `/system-log`，默认 24 小时过期。
  - `event_dispatch`：来源于事件派工，目标用户为 `receiveUser`，关联页面 `/event-disposal`，默认 7 天过期。
  - `work_order`：来源于工单接收 / 开始 / 完成，接收与开始发给 `receiveUser`，完成发给 `assignUser`，关联页面 `/event-disposal`，默认 7 天过期。
- 当前壳层消费约定：
  - 摘要弹层继续使用 `GET /api/system/in-app-message/my/page?pageSize=6` + `GET /api/system/in-app-message/my/unread-count`。
  - “查看更多”列表抽屉继续使用 `GET /api/system/in-app-message/my/page`。
  - 详情抽屉继续使用 `GET /api/system/in-app-message/my/{id}`。
  - 打开摘要弹层或详情抽屉不会自动改写已读状态；只有 `POST /api/system/in-app-message/my/read/{id}` 或 `POST /api/system/in-app-message/my/read-all` 才会更新未读统计。

### 5.9 帮助文档 `/api/system/help-doc`

- 管理端：
  - `GET /api/system/help-doc/page`
  - `GET /api/system/help-doc/{id}`
  - `POST /api/system/help-doc/add`
  - `PUT /api/system/help-doc/update`
  - `DELETE /api/system/help-doc/delete/{id}`
- 当前用户访问端：
  - `GET /api/system/help-doc/access/list`
  - `GET /api/system/help-doc/access/page`
  - `GET /api/system/help-doc/access/{id}`

补充口径：
- 文档分类当前固定为 `business / technical / faq`。
- 帮助文档访问当前同时受 `visible_role_codes` 和 `related_paths` 约束：
  - `visible_role_codes` 为空时，默认所有登录用户可见。
  - `related_paths` 为空时，不额外限制页面权限。
  - 当 `related_paths` 有值时，后端会按当前用户已授权菜单路径过滤无权页面资料。
- `GET /api/system/help-doc/access/list` 支持 `docCategory`、`keyword`、`currentPath`、`limit` 参数；其中 `currentPath` 主要用于提升“当前页面最相关资料”的排序优先级。
- `GET /api/system/help-doc/access/page` 支持 `docCategory`、`keyword`、`currentPath`、`pageNum`、`pageSize` 参数，返回 `PageResult<HelpDocumentAccessVO>`。
- 当前壳层消费约定：
  - 摘要弹层继续使用 `GET /api/system/help-doc/access/list?limit=6`。
  - “查看更多”列表抽屉统一使用 `GET /api/system/help-doc/access/page` 做服务端分页检索。
  - 详情抽屉继续使用 `GET /api/system/help-doc/access/{id}`，并保留 `currentPath` 排序与当前页相关标识。
- 若真实环境历史库尚未执行 `sql/upgrade/20260321_phase5_in_app_message_help_docs.sql`，`/api/system/help-doc/**` 与 `/api/system/in-app-message/**` 现在会返回明确业务错误；若旧库还未补齐通知中心后续治理字段，还需继续执行 `sql/upgrade/20260322_phase5_notification_center_followup.sql`；若本轮需要开启高优未读桥接，还需继续执行 `sql/upgrade/20260322_phase5_notification_channel_bridge.sql`；若需要查看桥接逐次尝试明细，还需继续执行 `sql/upgrade/20260322_phase5_notification_channel_bridge_attempt_log.sql`。

### 5.10 审计日志 `/api/system/audit-log`

- `GET /api/system/audit-log/list`
- `GET /api/system/audit-log/page`
- `GET /api/system/audit-log/system-error/stats`
- `GET /api/system/audit-log/business/stats`
- `GET /api/system/audit-log/get/{id}`
- `POST /api/system/audit-log/add`
- `DELETE /api/system/audit-log/delete/{id}`

## 6. 时序接口

- `GET /api/telemetry/latest?deviceId={deviceId}`（`spring-boot-iot-telemetry`）

说明：
- 当前接口已纳入活跃 Reactor 构建链路。
- 入参固定为 `deviceId`。
- 返回结构固定包含 `deviceId`、`deviceCode`、`productId`、`productKey`、`storageType`、`reportTime`、`traceId`、`properties`。
- 当 `iot.telemetry.storage-type=tdengine` 且 `iot.telemetry.read-routing.latest-source=v2` 时，接口当前优先从 MySQL 投影表 `iot_device_metric_latest` 读取 v2 latest；若 `legacy-read-fallback-enabled=true`，缺失指标会继续从 legacy stable + `iot_device_telemetry_point` 兼容结果补齐。
- 当 `iot.telemetry.storage-type=tdengine` 且读路由仍停留在 `legacy` 时，接口继续按产品物模型中的 `specsJson.tdengineLegacy` 映射从 legacy stable 查询最新一行，再按 `metric_code` 回填为标准属性；若 `legacy-normalized-fallback-enabled=true`，缺失指标继续从 `iot_device_telemetry_point` 补齐。
- v2 latest 路径下返回的 `reportTime / traceId` 优先取 `iot_device_metric_latest` 中最新 `reported_at / trace_id`；legacy 兼容结果仍按 `iot_message_log.trace_id` 与 `device_id + report_time` 的邻近时间窗口做最佳努力回补，当前兼容视图底层仍来源于物理表 `iot_device_message_log`。
- 当 `iot.telemetry.storage-type=mysql` 时，接口兼容回退到 `iot_device_property` 最新属性表。

## 7. 兼容与注意事项

- 消息日志统一对外口径为 `iot_message_log`；当前兼容视图底层仍写入物理表 `iot_device_message_log`。
- 部分“更新/删除”接口仍采用 `POST`（如风险点、规则、预案），属于当前历史风格，重构时需评估兼容性。
- OpenAPI 文档当前可作为联调与 SDK 生成输入，但对外发布口径仍需继续遵守 `代码 -> 本文 -> 运行时 OpenAPI` 的权威顺序。
